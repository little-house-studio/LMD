# Mermaid 空间编辑协作平台 PRD v0.6

## 1. 产品定位

- 面向企业内部的 Web 软件，部署在 Linux 服务器上，以网页形式提供访问、鉴权、存储和协作能力。
- 客户端负责主要编辑、渲染和交互计算，服务端主要负责文件存储、版本管理、协同同步、权限控制和扩展接口预留。
- 产品交互目标为「Figma 的画布操作感 + Project Graph 的结构感 + Mermaid/Markdown 的标准文本兼容」。
- 产品形态应更接近“云端项目工作台”而非单文件编辑器，用户打开的是项目、文件夹、文档与图块组成的工作空间。
- 核心工程文件应收敛为单个 `.md` 文件；标准 Mermaid 与普通 Markdown 正文共存，自定义兼容层放在文档尾部。
- 云端项目文件是协同主真相源，同时也必须支持直接打开本地项目作为独立编辑器工作区使用。
- 第一阶段聚焦 `flowchart`，不追求一次性覆盖全部 Mermaid 图类型。

## 2. 设计原则

### 2.1 标准优先

- `.md` 文件是工程真相源，主体只使用标准 Markdown 与标准 Mermaid 代码块。
- 不在 Mermaid 源码中写入任何私有坐标、私有注释、私有扩展字段。
- 任何第三方 Mermaid 编辑器至少都能打开并正确理解语义内容。

### 2.2 画布优先

- 默认主模式是画布模式，不让用户长期停留在源码界面。
- 源码编辑是独立模式，而不是常驻侧栏。
- 用户应能像在 Figma 中一样快速框选、拖拽、连接、对齐和组织结构。
- 画布操作必须原生兼容 MacBook 触控板、Magic Trackpad 和常见鼠标滚轮，不要求用户切换系统手势习惯。
- 画布缩放、平移、手势和快捷键缩放不得触发浏览器页面级缩放或页面级滚动。

### 2.3 沉浸优先

- 中央画布应始终是第一视觉主体，默认占据工作区的绝大多数面积。
- 工作台应采用“固定骨架 + 可折叠面板 + 少量悬浮工具”的混合结构，而不是所有 UI 都漂浮在画布上。
- 顶部工作区栏、左侧窄导航、主侧栏、右侧属性栏在桌面端应优先采用固定停靠式布局，但必须可以折叠或收起。
- 只有状态芯片、底部工具坞和临时操作层适合采用悬浮形式。
- 非必要 UI 应遵循“需要时出现，不需要时退后”的策略：
  - 默认隐藏或半隐藏
  - 选中时出现
  - 操作完成后自动退场或允许一键收起
- 布局目标更接近 Blender / Figma 这类专业创作工具的沉浸式工作台，而不是仪表盘式后台。
- 安卓竖屏网页端必须采用独立工作台策略，而不是把桌面布局直接缩小：
  - 画布始终作为唯一主舞台
  - 高频操作优先落在底部可达区
  - 面板通过抽屉和底部 sheet 临时进入
  - 顶部只保留极少量全局信息和保存状态

### 2.4 轻量优先

- 产品首先是公司内部协作工具，不做浏览器扩展、不做插件市场、不做 Electron 桌面壳。
- 服务器不承担大规模图形运算，绝大多数渲染和布局运算在浏览器本地完成。
- 功能设计优先减少模式切换和界面噪音，避免工具臃肿。

### 2.5 协同与平台优先

- 文档应支持多人同时编辑、评论、回溯和权限控制。
- 项目、文件夹、文档和 Mermaid block 都应存在清晰的云端归属与权限模型。
- 协同、扩展接口与插件能力不是外挂，而是平台能力的一部分。
- 对外能力优先通过 SDK、命令系统和服务接口提供，而不是直接暴露底层实现。

## 3. 用户价值

### 3.1 使用者视角

- 快：创建节点、连线、分组、切换方向、导出文档都要快。
- 顺：交互应接近 Figma，少弹窗、少表单、少复杂设置。
- 简：默认界面只保留最常用能力，把高级能力放到右侧属性区和命令面板。
- 通用：既能画流程图、架构图、知识图谱，也能承接项目关系图与人工整理后的结构沉淀。

### 3.2 开发者与平台视角

- 稳：标准 Mermaid 语义不能被工作区特性污染。
- 可扩展：后续可增加更多图类型、布局算法、协同能力和外部工具能力。
- 可集成：支持 SDK、服务 API、命令调用和自动化流程。
- 可治理：具备权限、版本、审计和插件隔离能力。

## 4. 产品边界

### 4.1 V1 必须支持

- `flowchart` / `graph TD|LR|RL|BT`
- 节点与连线的可视化编辑
- 子图分组（`subgraph`）与嵌套
- 标准 Markdown / Mermaid 导入导出
- 单 Markdown 工程格式与 compat layer 的布局恢复
- 画布模式与源码模式切换
- 多人协同编辑
- 安卓竖屏网页端沉浸式编辑壳
- 云端项目 / 文件夹 / 文件资源管理器
- 版本历史
- 扩展/自动化的 SDK 与接口设计

### 4.2 V1 明确不做

- 浏览器扩展
- 插件市场
- Electron / 原生桌面客户端
- 贝塞尔/直角路由手工控制点
- 非标准字体、图标上传、任意视觉图层效果
- 一次性支持全部 Mermaid 图类型

### 4.3 VSCode 插件变体

- 产品需要支持一个本地优先的 VSCode 插件版本：
  - 名称：`LMD_EDITER`
  - 目标文件：`*.lmd`
- VSCode 插件版聚焦“本地画布编辑器”：
  - 删除 AI
  - 删除云端
  - 删除协作
  - 删除内置文件管理器
  - 删除历史视图
  - 删除内置源码编辑界面
- VSCode 插件版应保留：
  - 画布编辑
  - 图谱树
  - 属性栏
  - 导航图
  - 整理 / 布局 / 标准化
  - 导出当前完整画布图片
- `*.lmd` 文件在 VSCode 中应默认进入画布界面。
- 点击右上角 `源码` 按键时，应切回 IDE 自带文本编辑器，而不是打开插件内部源码面板。
- 插件版 UI 应采用极简工具栏策略：
  - 顶部尽量使用图标按钮
  - 文案尽量通过 tooltip 提供
  - 视觉语言与 Notion 夜间版一致，但密度更高、更像 IDE 工具带

## 5. 文档与数据模型

### 5.1 核心原则

- 一个工程只对应一个 Markdown 文件。
- 文件正文保持 Markdown 标准结构。
- Mermaid 图使用标准 fenced code block。
- 工具私有信息统一进入文档末尾的 compat layer，不再拆分独立 sidecar 文件。

### 5.2 单文件工程格式

- 推荐对该格式使用正式名称：
  - `LMD`
- 推荐全称表述：
  - `LMD (LTHS Markdown Document)`
- 命名原则：
  - `LMD` 是该单文件协议的格式名
  - 其物理文件后缀继续保持标准 `.md`，不新增私有扩展名
  - 对外表述时应强调它继承自标准 Markdown 文件，而不是自造二进制或私有容器
  - 不建议直接把 `Mermaid` 放进格式名，以避免与 Mermaid 官方语法或官方格式产生混淆
- 推荐对外表述：
  - `This file is an LMD document, stored as a standard Markdown (.md) file.`

- 推荐结构如下：

```md
# Project Name

## Summary

User defined summary.

## Diagram

```mermaid
flowchart LR
  A[Start] --> B[Review]
```

## Content

Any normal Markdown content can live here.

## Node Annotations

### `A`

Owner: Platform

### `B`

Keep this step manual.

```lths-compat
v1
```
```

- 字段与标题命名建议使用英文格式：
  - `Project Name`
  - `Summary`
  - `Diagram`
  - `Content`
  - `Node Annotations`
  - `lths-compat`
- 该文件必须可以直接被 GitHub、Mermaid Live Editor、Obsidian、Notion 等兼容 Mermaid 的环境读取其 Mermaid 语义内容。
- 运行时只接受 `.md` 工程文件。
- 旧的裸 Mermaid 文本、双文件工程和旧兼容结构不再作为主编辑入口直接解析。
- 若用户打开的是非标准 Markdown 工程，必须通过“标准化格式”动作迁移到当前单文件工程结构。
- “标准化格式”动作必须具备幂等性：
  - 已经符合当前单文件工程结构的文档，再次执行标准化时不得重复生成 `Summary / Diagram / Content` 章节
  - 若存在重复章节或旧版错误生成的残留章节，标准化应负责自动折叠清理为单一标准结构
- 每个工程在画布中都必须同步存在一个“附加信息框”：
  - 数据源来自文件中的 `## Content` 区块
  - 它不可删除
  - 它不可参与连线
  - 它必须能在画布中直接编辑
  - 展示时必须按 Markdown 预览方式渲染，而不是纯文本
  - 应兼容旧内容中的 `<br/>` 并按正常 Markdown 换行呈现
  - 默认固定在画布左上方向的独立信息位，不参与普通节点自由排布
  - 尺寸必须随内容长度自动变化，但在超长内容下允许通过内部滚动阅读
  - 它必须支持折叠 / 展开
  - 折叠状态应被保存并在重新打开时恢复

### 5.3 Compat Layer

- compat layer 采用 Markdown fenced block，自定义语言标识使用英文：
  - `lths-compat`
- compat layer 只承载兼容层信息，不写回 Mermaid 正文。
- compat layer 的首要目标是低字符数、低 token 消耗和稳定序列化，不追求人类可读性。
- 新写入格式必须优先使用短串序列化，不要求人工可读。
- 旧 compat 表达只允许在“标准化格式”动作里做迁移读取，主解析链路不再长期兼容。
- compat layer 应优先采用：
  - 短 key
  - 压缩结构
  - 数组优于冗长对象
  - 单行或最小空白输出
- 当 compat layer 省略节点宽高时，编辑器在重新打开文档时必须根据节点内容重新计算尺寸，不能回退为 0 或极小值。
- compat layer 可包含：
  - 文档版本号
  - 节点坐标、尺寸、折叠状态
  - 子图边界框
  - 视口位置、缩放级别
  - 本地文件编辑器相关能力开关
  - 评论锚点与编辑器专用路由提示
- 若 compat layer 缺失，系统应能仅依据标准 Mermaid 自动重新布局并继续编辑。

### 5.4 云端项目与文件模型

- 系统中的对象层级建议为：
  - `Workspace / Team`
  - `Project`
  - `Folder`
  - `Markdown File`
  - `Mermaid Block`
  - `Markdown File`

- 用户打开应用后默认进入项目空间，而不是直接进入单一文件。
- 左侧资源管理器展示的是云端项目树：
  - 项目
  - 文件夹
  - Markdown 文件
  - 路径与 Git 跟踪状态

- 云端是持久化真相源：
  - Markdown 正文保存在云端项目文件中
  - compat layer 随 Markdown 一起保存于同一文件末尾
  - 评论、权限、版本和协同 presence 均挂在云端文档对象上

- 本地项目模式是并列工作模式：
  - 用户可以直接打开本地项目目录中的 Markdown 文件进行编辑
  - 本地项目直接编辑真实 `.md` 文件，不拆分 sidecar
  - 本地项目不依赖云端才能进入编辑
  - 若处于云端协同文件中，网络恢复后仍以云端版本和协同状态为合并基础

- 文件标识建议同时具备：
  - 全局文件 ID
  - 项目内路径
  - 当前版本号
  - 当前协同会话 ID

## 6. 标准兼容策略

### 6.1 标准模式

- 所有可导出的 `.md` 内容必须是标准 Mermaid 语法。
- 默认导出保留单文件工程结构；compat layer 放在文档尾部。
- 编辑器必须支持一键导出“当前完整画布图片”：
  - 导出对象是画布内容本身，而不是整个浏览器界面
  - 导出范围应按当前图内容边界自动裁切
  - 需包含节点、分组、连线、标签、附加信息框和画布背景网格
  - 默认导出格式为 PNG

### 6.2 编辑器增强模式

- 自由布局、吸附、视口、局部排列等能力只在编辑器内生效。
- 这些能力保存在 compat layer 中，不回写到 Mermaid 源码。

### 6.3 兼容等级

- `Strict Mermaid`
  - 只允许最稳定、最通用的 flowchart 语法。
- `Modern Mermaid`
  - 允许较新的 Mermaid 能力，但仍要求标准语法。
- `Workspace Enhanced`
  - 允许使用 compat layer 中的增强布局信息，但导出 `.md` 时仍回到标准内容。

## 7. 分组、层级与 Project Graph

### 7.1 标准语义分组

- V1 的“分组”优先映射为 Mermaid `subgraph`。
- 支持：
  - 创建子图
  - 重命名子图
  - 子图内放置节点与边
  - 嵌套子图
  - 子图方向设置

### 7.2 层级定义

- 文档层级不再按 Figma 的纯视觉 z-index 理解，而是按以下三层定义：
  - 文档层级：Markdown 文件中的 Mermaid block
  - 图结构层级：subgraph、node、edge
  - 工作区层级：画布选中、折叠、锁定、视口

- V1 不将“置顶/置底”作为标准语义承诺。
- 若后续需要视觉层级，仅作为工作区状态存在，不写入 Mermaid。

### 7.3 Project Graph 视图

- 左侧工作区不应只有单一面板，而应是 Figma 风格的双层侧栏：
  - 最外层窄导航栏：文件、图谱、最近文件、个人状态
  - 主侧栏：资源管理器、Project Graph、搜索结果

- `Project Graph` 保留，但不再承担全部导航职责。
- `Project Graph` 应包含三类信息：
  - 文档树：项目、文件夹、Markdown 文件、Mermaid block
  - 结构树：subgraph 与 node 混合的单一树形层级
  - 关系视图：节点之间的连接关系、跨子图引用关系

- 图谱 / 图层面板要求：
  - 采用单一树形图结构
  - 分组和元素不允许拆成两个独立列表
  - 节点应显示在所属分组之下
  - 根节点应直接显示在根层
  - 顶部必须提供紧凑导航图，用于快速定位与移动当前画布视口
  - 导航图至少支持点击跳转和拖动视口框移动
  - 点击任意树节点后，画布应定位并选中对应对象

- 目标不是模仿 Figma 图层的全部行为，而是形成相似的“桌面工作台信息密度”：
  - 左边专注导航和资源
  - 中间专注画布
  - 右边专注属性和协同信息

### 7.4 云端资源管理器

- 左侧资源管理器需支持：
  - `云端项目` 与 `本地项目` 两个清晰分区
  - 项目列表
  - 文件夹树
  - Markdown 文件列表
  - 路径视图
  - 最近打开
  - 收藏与置顶

- 资源管理器需具备典型企业项目操作：
  - 新建项目
  - 新建文件夹
  - 新建 Markdown 文件
  - 重命名
  - 移动
  - 复制路径
  - 分享
  - 查看权限
  - 查看版本历史

- 文件状态需在资源管理器中可见：
  - 谁正在查看
  - 谁正在编辑
  - 是否有未读评论
  - 是否有未提交本地改动
  - 是否有冲突待处理

- 云端项目区应按项目路径排布文件，不应把同一工程拆成 sidecar、缓存等多个伪文件。
- 本地项目区应按真实本地路径排布文件，不应伪装成缓存列表。
- 本地文件区应提供文件管理器式操作：
  - 直接打开 `.md` 文件
  - 打开目录
  - 新建 Markdown 工程文件
  - 重命名 Markdown 工程文件
  - 删除 Markdown 工程文件
  - 一键标准化当前文件格式
  - 返回上一级目录
  - 路径面包屑跳转
  - 当前目录视图
  - 当前目录列表默认只显示目录与 `.md` 文件，不显示无关文件类型

- 左侧文件区在桌面端应尽量接近常规文件管理器的编排逻辑：
  - 紧凑工具条
  - 路径栏
  - 上一级按钮
  - 当前目录列表
  - 不应用大块卡片和大面积留白吞掉侧栏空间

### 7.5 多文件标签与工作区会话

- 应支持 Figma 风格的多文件标签工作流，而不是一次只打开一个文件。
- 标签栏行为建议包括：
  - 打开文件后在顶部生成标签
  - 标签支持固定、关闭、关闭其他、重新打开最近关闭
  - 标签显示文件名、未保存状态、在线协同人数、权限状态
  - 同一文件的不同 Mermaid block 可在同一标签内切换，不建议每个 block 单独变成浏览器页签

- 工作区会话需记忆：
  - 最近打开的项目
  - 最近展开的文件夹树
  - 最近打开的文件标签
  - 当前选中的 Mermaid block
  - 侧栏折叠状态
  - 画布视口与源码滚动位置

- 当用户重新进入应用时，应尽可能恢复上次工作现场，但恢复顺序需遵循：
  - 云端最新文件版本
  - 本地项目或本地草稿差异检查
  - 最近工作标签恢复

## 8. 界面与交互

### 8.1 整体界面

- 交互骨架应保持专业创作工具的工作台逻辑，但视觉语言应整体切换为 `Notion 夜间版` 风格：
  - 高信息密度
  - 低视觉噪音
  - 中性灰黑主色，不使用高饱和大面积渐变
  - 清晰分区而非卡片堆叠
  - 控件紧凑但不拥挤
  - 表层视觉应更接近文档工作台，而非仪表盘或营销页

### 8.1.0 Notion 夜间版设计语言

- 颜色策略：
  - 主背景以 `#191919` 一类近黑中性灰为基底
  - 面板层级应使用 `#202020 / #252525 / #2b2b2b` 一类相近灰阶递进
  - 主文字应接近暖白或中性白，弱文字退到中灰
  - 强调色以单一蓝色系为主，不同时混入多套高饱和品牌色

- 组件语言：
  - 面板、输入框、列表项、工具条都应采用薄边框、低阴影、小圆角
  - 大多数组件不应依赖玻璃模糊、厚阴影、大外发光来建立层级
  - 激活态应优先通过细描边、轻底色和文字提亮表达，而不是厚重按钮皮肤

- 排版策略：
  - 字体以系统无衬线为主，避免花哨字体
  - 标题、说明、分组标签应维持清晰但克制的层级
  - 顶栏、侧栏、属性栏文本密度应接近 Notion 桌面端，而不是移动端放大式布局

- 空间与密度：
  - 页面应更像“文档与编辑器工作台”，而不是“卡片式后台”
  - 面板内优先使用列表、分隔、细条和小型工具按钮
  - 不允许把每一块信息都包装成大面积独立卡片

- 画布与 UI 关系：
  - UI 外壳使用 Notion 夜间版风格，但画布交互仍保留专业图编辑器能力
  - 画布上的状态条、模式切换和工具坞也应服从同一套中性灰视觉
  - 节点、分组、附加信息卡若存在业务配色，仍应控制阴影、描边和圆角语言一致

- 顶部工作区栏：
  - 左侧只保留一个极简品牌 Logo，不显示团队、工程类型或当前文件名块
  - 点击左上角 Logo 应打开一份面向用户的操作说明面板
  - 操作说明应使用简短语言与 emoji，覆盖高频画布交互和快捷键
  - 多文件标签与路径信息应交给中部和命令区承担，不应占据左上角品牌区域
  - 命令组：资源、编辑、视图
  - 所有桌面端顶栏按钮、分组标签、状态芯片都必须保持单行显示，不得出现纵向换行、字符堆叠或按钮高度失衡
  - `资源` 组需包含云端文件入口、本地图谱工程入口与导出入口
- `编辑` 组需包含新建节点、复制、分组、删除、整理、布局、标准化等高频命令
  - `视图` 组需包含画布、源码、属性、聚焦等切换命令
  - 在线成员头像与协同状态
  - 复制、导出、版本、命令入口
  - 整体应为固定停靠的轻量命令条，不应漂浮遮挡画布

- 左侧双层区域：
  - 一级窄栏：
    - 文件
    - 图谱
    - 最近文件
    - 用户菜单
  - 二级主侧栏：
    - `Files`
    - `Graph`
    - `Search`

- 桌面端左侧不单独提供“协作面板”作为一级主导航。
- 协作信息保留在：
  - 顶部在线成员区域
  - 右侧属性 / 会话区域
  - 历史与评论上下文

- 中央主区：
  - 无限画布
  - 网格背景
  - 缩放与平移
  - 迷你地图
  - 顶部轻量状态芯片
  - 底部浮动工具坞
  - 面板关闭后的全沉浸主舞台

- 右侧边栏：
  - 属性面板
  - 布局面板
  - 协同信息
  - 版本 / 评论上下文

- 画布模式下的整体布局策略必须是：
  - 中央主舞台为固定基础层
  - 顶部工作区条、左侧窄导航、主侧栏、右侧属性栏在桌面端应优先为固定停靠层
  - 顶部状态芯片和底部工具坞可为悬浮层
  - 不允许使用后台式大表单面板吞掉主画布面积
  - 不允许出现“所有东西都悬浮”导致视觉漂浮和主次失衡

### 8.1.1 版式、间距与视觉密度

- 不要求复制 Figma 的具体控件，但应采用相近的桌面创作工具尺度：
  - 顶栏高度：`48-56px`
  - 左侧一级窄栏宽度：`36-48px`
  - 左侧主侧栏宽度：`164-280px`
  - 右侧属性栏宽度：`156-248px`
  - 底部浮动工具坞高度：`48-56px`

- 间距系统使用 `4 / 8 / 12 / 16 / 24 / 32` 递进：
  - 面板内部优先 `6px`、`8px` 或 `12px`
  - 组件间小间距优先 `4px` 或 `6px`
  - 大区块之间优先 `16px` 或 `24px`

- 控件密度要求：
  - 工具按钮高度建议 `26-32px`
  - 主按钮高度建议 `32-36px`
  - 面板标题区上下留白小而稳定
  - 属性栏内部的输入框、页签、样式预设、颜色控件应明显比画布节点和模态面板更紧凑，接近桌面文件管理器与检查器密度
  - 文本、图标、列表项排列尽量紧凑
  - 高频工具优先使用图标，名称在 hover、focus 或展开态下再显示
  - 不允许把每个小功能都包装成占据大量面积的大卡片
  - 左侧资源树、图谱树、属性项应优先采用列表式和分隔式排布，而不是仪表盘式卡片堆叠

- 视觉风格要求：
  - 使用薄分隔线、低对比面板背景、有限圆角
  - UI 风格应统一为单一的克制工具风格，不允许一部分组件完全方角、另一部分组件又采用大圆角
  - 避免大面积玻璃态、夸张渐变、过多阴影
  - 保持专业工具感，而非营销页或仪表盘感
  - 允许顶栏标签、侧栏列表和属性栏采用更紧凑的桌面应用密度
  - 节点样式预设必须提供明显的色相、明度和饱和度差异，避免全部落在低对比浅色区间

- 画布本身应是视觉中心：
  - 中央区域留给内容，而不是被表单和说明侵占
  - 非必要说明不应以 notice 或 toast 形式打断主操作流
  - 常用工具集中在底部浮动工具坞和右侧属性栏
  - 在默认画布态下，主画布视觉占比目标应达到 `90%-95%`
  - 浮层面板打开后也不应形成永久结构切栏，而应以覆盖式方式临时出现
  - 桌面端整个工作台必须完整落在浏览器当前视口内，不应因为布局自身超高而要求用户滚动浏览器页面才能看到完整 UI
  - 需要滚动时，应优先由内部面板或内部列表滚动，而不是由浏览器页面整体滚动

- 沉浸式视觉要求：
  - 侧栏和属性栏默认可收起
  - 未选中状态下不强行展示大面积属性面板
  - 面板开合应有短促、稳定、低打扰的动画
  - 状态信息优先压缩成胶囊标签、细条或小型悬浮卡片
  - 高使用频率的导航和工具应尽量图标化，减少持续文字噪音

### 8.1.2 面板行为与工作区布局记忆

- 左右侧栏都应支持：
  - 拖拽调整宽度
  - 折叠 / 展开
  - 单独隐藏
  - 宽度记忆
  - 桌面端优先固定停靠，移动端或窄屏再切为覆盖式抽屉
  - 拖拽改宽度时应显示明确的边界拖拽热区，并在拖拽结束后记住宽度

- 窄导航栏应始终保持稳定位置，不随主侧栏折叠而消失。
- 窄导航栏应优先使用图标表示，鼠标悬停、键盘 focus 或展开时再显示名称提示。
- 右侧属性栏在以下情况下应有明确行为：
  - 桌面端属性栏只服务当前选中对象，不承载会话、文档统计、扩展动作、导出入口等杂项信息
  - 无选中对象时，属性栏应切换为工程级信息编辑面板，允许直接编辑 `Project Name`、`Summary`、`附加信息`
  - 属性栏不得因为默认宽度过大而压缩主画布，应优先采用窄而密的检查器风格
  - 选中节点时只显示节点属性
  - 节点属性面板必须提供至少 10 组可一键套用的样式预设，用于快速设置填充、描边和文字颜色
  - 选中连线时只显示边属性
  - 选中子图时只显示子图属性
  - 属性面板中的文本、数字等连续输入字段应先进入本地草稿态，在失焦、确认或结束一次编辑动作后再提交，不得每输入一个字符就保存一次
  - 多选时仅显示批量属性与必要操作，不回退到文档信息面板

- 桌面端左右侧栏的开合规则：
  - 不使用单独占据高度的侧栏标题栏
  - 左侧栏和右侧栏都应在面板边缘提供窄三角开关
  - 点击边缘三角即可展开或收起对应侧栏
  - 三角开关应固定贴在侧栏边界上，而不是漂浮在画布内部

- 左侧资源区搜索规则：
  - 左侧只保留一个搜索输入
  - 文件视图与图谱视图共用这一搜索输入
  - 不允许在同一左侧工作流里重复出现多个语义相近的搜索栏

- 中央画布区应优先保留最大可用空间：
  - 当窗口缩小时，优先压缩侧栏而不是画布
  - 小屏设备下主侧栏与右侧栏可切为抽屉模式
  - 在桌面端，默认优先采用固定停靠式面板；在窄屏再切换为覆盖式面板

- 工作区布局需支持自动记忆：
  - 当前侧栏宽度
  - 当前折叠状态
  - 当前显示的侧栏标签页
  - 当前工具坞展开状态
  - 上一次面板是否处于悬浮展开
  - 上一次属性栏是否因选中对象而自动弹出

### 8.1.3 安卓竖屏沉浸式工作台

- 安卓竖屏网页端不得沿用桌面三栏布局缩小版，而应采用独立的移动工作台结构。
- 目标屏幕范围：
  - 宽度优先覆盖 `360px - 480px`
  - 高度优先覆盖 `720px - 960px`
  - 优先适配 Chrome Android
  - 同时兼容常见 Chromium 内核 WebView

- 安卓竖屏工作台应拆为五层：
  - 顶部轻量 App Bar
  - 中央全屏主舞台
  - 贴边状态层
  - 底部模式与工具层
  - 覆盖式抽屉 / 底部 Sheet 层

- 安卓竖屏默认视觉分配要求：
  - 主舞台可见面积目标不低于 `92%`
  - 常驻非画布 UI 面积目标不高于 `8%`
  - 在非编辑态和无面板展开态下，画布需尽量贴满除安全区外的全部空间

- 安卓竖屏的顶部轻量 App Bar：
  - 高度建议 `44-52px`
  - 只保留：
    - 返回或项目入口
    - 当前文件名
    - 保存状态
    - 协作人数入口
    - 更多菜单入口
  - 不应在顶部长期展示大量标签、帮助文案和次要按钮

- 安卓竖屏的中央主舞台：
  - 画布模式下占据几乎全部主体区域
  - 默认只展示网格、节点、边、分组和极少量状态芯片
  - 默认不展示桌面式左右固定栏
  - 非操作状态下不显示说明卡片

- 安卓竖屏的底部模式与工具层：
  - 固定在底部安全区上方
  - 包含两级信息：
    - 一级为模式切换：`画布` / `源码` / `历史`
    - 二级为当前模式下的高频工具
  - 画布模式下优先保留：
    - 选择
    - 新建节点
    - 创建分组
    - 重置视口
    - 缩放
    - 打开属性
  - 图标优先，名称在选中、长按或展开时显示

- 安卓竖屏的抽屉与底部 Sheet：
  - 左侧资源管理器改为全高覆盖抽屉
  - 右侧属性区改为底部 Sheet
  - 点击底部 `文件` 按钮时必须直接打开 `文件` 抽屉，而不是复用当前左侧面板状态
  - 点击底部 `属性` 按钮时，如果当前选中了节点、连线或分组，主舞台应先将该对象移动到上半屏可见焦点区，再弹出下半屏半高属性 Sheet
  - 属性 Sheet 在安卓竖屏下应采用悬浮半框样式，默认高度约为屏幕 `45% - 55%`，底部保留与模式栏的安全间距
  - 协作和评论优先采用底部 Sheet 或全屏二级页
  - 抽屉与 Sheet 不应同时全开，避免遮挡主舞台

- 安卓竖屏的信息架构建议：
  - 一级模式：
    - 画布
    - 源码
    - 历史
  - 二级面板：
    - 文件
    - 图谱
    - 属性
    - 协作
  - 一级模式固定在底部
  - 二级面板通过顶部入口、长按工具或底部扩展按钮拉起

- 安卓竖屏的间距与尺寸建议：
  - 页面外边距：`8px - 12px`
  - 底部工具条高度：`52px - 60px`
  - 主要触控目标最小尺寸：`44px`
  - 重要按钮建议 `48px`
  - 底部 Sheet 顶部圆角：`20px - 24px`
  - Sheet 内部主间距：`12px`

- 安卓竖屏的沉浸式原则：
  - 所有非必要名称、说明、帮助文字默认收起
  - 优先使用图标、选中态、颜色和结构层级传达信息
  - 状态以小型胶囊和点状 presence 表达，不占主舞台
  - 面板拉起后仍应保留一部分主舞台上下文，不应完全迷失当前画布位置
  - 底部模式栏和画布工具条必须保持紧凑居中，不得出现图标分布松散、按钮错位或无意义大空白
  - 交互必须经过专项排查，避免误触双击、面板卡死、手势取消后状态残留、底部工具条遮挡等安卓常见问题

- 安卓竖屏的源码模式应采用独立工具壳：
  - 顶部为文件名与保存状态
  - 中部为全屏编辑器
  - 底部为模式栏与预览切换入口
  - Mermaid 预览应使用半屏上拉或全屏切换，而不是与编辑器强行左右并排

- 安卓竖屏的历史模式应采用时间线列表页：
  - 以卡片流或时间线流为主
  - 支持筛选“全部 / 我 / 协作者 / 系统”
  - 点击历史记录可定位回画布对象

### 8.2 模式切换

- 默认进入 `画布模式`。
- 默认指针行为为 `选择模式`，不单独提供“移动工具”作为主模式。
- 在选择模式下：
  - 从空白区域按住左键拖动，进入框选。
  - 从节点或已选对象按住左键拖动，进入移动。
  - 从连接锚点拖动，进入连线创建。

- 画布模式：
  - 以拖拽、框选、连线为主。
  - 面板应默认尽量收敛，用户通过窄导航栏、顶部浮层或快捷键主动展开资源和属性。
  - 默认应呈现“主舞台 + 少量状态芯片 + 浮动工具”的构图，而不是说明面板优先。
  - 节点在画布中只显示节点内容，不显示内部 ID 或名称栏。
- 源码模式：
  - 整屏切换为编辑器视图，类似代码编辑器。
  - 提供语法高亮、补全、错误提示、格式化和兼容性检查。
  - 仍然保留与画布模式一致的工作台骨架和模式切换位置
  - 变化的只是中央主舞台内容，而不是整套外壳重排
- 历史模式：
  - 查看版本、差异、评论锚点和操作记录。
  - 同样保持与画布模式一致的桌面骨架，仅主舞台内容切换为时间线视图

### 8.3 交互状态模型

- 画布需显式区分以下交互状态：
  - `Idle`
  - `Hover`
  - `Selected`
  - `Editing`
  - `Dragging`
  - `BoxSelecting`
  - `Connecting`
  - `Panning`
  - `Readonly`

- 任一时刻只允许一个主状态生效，避免“正在编辑文本时又触发拖拽”这类冲突。
- 交互切换需有清晰反馈：
  - Hover 高亮
  - 选中描边
  - 拖拽预览
  - 连线预览
  - 锁定或只读标识

### 8.4 选择模型

- 默认选择模式下的行为规则：
  - 单击空白区域：
    - 清空当前选择。
- 在空白区域按住左键拖动：
    - 进入框选。
  - 框选命中的对象必须立即进入高饱和高对比高亮态，明显区别于 hover。
  - 单击节点：
    - 选中节点。
  - 拖拽节点：
    - 若节点未选中，则先选中再移动。
    - 若节点已在多选集中，则整体移动选区。
  - 单击连线：
    - 选中连线。
  - 单击子图边框：
    - 选中子图。

- 多选规则：
  - `Shift + Click` 将对象加入或移出当前选区。
  - `Shift + Drag` 会对框选结果执行加选 / 减选切换，而不是简单覆盖当前选区。
  - `Esc` 清空选区并退出临时交互状态。

- 选中优先级建议：
  - 文本编辑态优先
  - 节点优先于连线
  - 连线优先于子图背景
  - 子图优先于画布空白

- 重叠对象选中时，应支持：
  - 首次点击选择最上层可交互对象
  - 再次点击在重叠对象间轮换
  - 右键菜单列出当前指针处可选对象

### 8.5 画布核心交互

- 双击空白创建节点。
- 双击空白创建的节点默认为未链接节点。
- 若双击空白位置位于某个组内部，新节点必须直接创建在该组里。
- 中键按住拖拽应直接平移画布，且优先级与 `Space + Drag` 一致。
- 从节点锚点拖出可直接创建连线。
- 右键按住节点或分组本体并拖动，也必须直接进入连线创建。
- 右键从单个节点 / 分组拖到空白释放时，应创建一个新节点并自动建立连接。
- 右键从多选节点集合中的任一节点拖出时，应视为“多来源汇聚”：
  - 拖到已有节点或分组时，所有被选节点都应连接到同一目标
  - 拖到空白释放时，应创建一个新节点，并让所有被选节点汇聚指向它
- 右键拖线时，按住 `Ctrl/Cmd` 应切换为普通无箭头线。
- 拖到空白处释放时，可自动创建新节点并建立连接。
- 若从组内节点拖线并落到该组内部空白区域，不应连接到组本身，而应在该组内部创建一个新的同级节点并建立连接。
- 若从组内节点拖线并落到组外空白区域，应在外层画布创建新节点并建立连接。
- 按住 `Ctrl/Cmd` 拖拽单个节点或单个组到一条已有连线中间时，应进入“插入连线”模式：
  - 若原连线为 `A -> C`，插入 `B` 后应自动改写为 `A -> B -> C`
  - 原连线的标签文字、线型与样式应默认保留在前半段 `A -> B`
  - 新增的后半段 `B -> C` 默认不带标签，但继承原连线线型与样式
  - 拖拽悬停时必须提供明确预览，显示插入后的双段连线路径
- 连线标签必须支持多行长文本：
  - 双击连线后进入原位多行编辑
  - `Enter` 直接提交标签修改
  - `Shift + Enter`、`Ctrl + Enter`、`Alt + Enter` 插入换行
  - 保存、刷新、重新打开文件后必须完整保留换行内容
- 画布中必须始终存在一个不可删除的“附加信息框”，作为 `Content` 区块的可视化承载：
  - 默认参与选择、聚焦和属性编辑
  - 不参与节点分组语义
  - 不提供连线锚点
  - 在节点视图中以 Markdown 预览形式显示
  - 双击或在属性栏中可进入内容编辑
  - 在没有选中任何对象时，右侧属性栏也必须允许直接编辑同一份附加信息数据
  - 正文区域应允许滚轮和触控板滚动，且不得把滚动事件透传给画布
  - 不再允许用户拖动其位置；位置由工作台固定策略决定
  - 编辑态必须保证长文本可完整阅读，必要时通过内部滚动承载
  - 标题栏应提供折叠 / 展开操作，折叠后保留最小摘要信息
- 连线从节点出入口离开时，默认应使用自然曲线延伸，而不是生硬的短直线桩段。
- 同一节点同一侧的多个连线端点必须自动避让，根据方向与角度分布顺序错开，避免全部汇集到同一个点。
- 同一对节点的双向连线不仅中段要分离，起点和终点附近也必须稳定分侧，避免在连接点附近重新缠绕或打结。
- `Enter` 编辑节点文本。
- 节点内容若包含换行，保存、刷新、重新打开文件后也必须完整保留，不得回退为节点 ID 或丢失后续文本。
- 选中单个节点且未进入编辑态时，`Tab` 必须创建一个与当前节点自动连线的新节点，并立即把焦点切到新节点的原位编辑态。
- 选中多个节点且未进入编辑态时，`Tab` 必须为每个被选节点分别创建一个新的相邻节点，并各自建立独立连线，不得只创建一个共享节点。
- 选中单个节点且未进入编辑态时，`Space` 必须创建一个当前层级的同级新节点，放在不与当前节点重叠的空白位置，并立即进入该节点的原位编辑态。
- `Shift + Tab` 提升层级或移出当前子图。
- `Space + Drag` 平移画布。
- 滚轮缩放，支持聚焦缩放。
- 框选、多选、对齐、分布、复制、删除。
- `Delete` 或 `Backspace` 删除当前选中对象。
- `Alt + Drag` 复制当前被选节点或多节点，并保留与原节点一致的连接关系：
  - 选区内部连线必须完整复制
  - 选区与外部节点之间的入边 / 出边也必须复制到新节点上
- `Ctrl/Cmd + C` 复制被选节点与其内部连接。
- `Ctrl/Cmd + V` 粘贴复制出的节点与内部连接，并自动偏移位置。
- 命令面板用于快速执行“创建子图”“切换方向”“自动整理”等动作。

- 节点 Hover 时应显示：
  - 可连接锚点
  - 快速创建入口
  - 基础摘要信息

- 选区形成后应显示上下文快捷条，提供高频操作：
  - 创建子图
  - 对齐
  - 整理选区
  - 评论
  - 删除

- 选中态可视反馈必须明显强于 hover 态：
  - 节点应有明确高亮描边、外发光或选中环
  - 分组容器应有明显整体高亮，而不是只有细边变化
  - 连线选中时，线体与标签都应同步强化，避免只亮一部分
  - 连线选中应带独立高亮轨迹或选择光带，即使默认线很细也必须一眼可见

- 右键菜单按对象上下文变化：
  - 节点：重命名、复制、删除、创建关联节点、创建子图、评论
  - 连线：编辑标签、反转方向、删除、评论
  - 子图：重命名、折叠、展开、解散、评论
  - 空白：新建节点、粘贴、整理全图、重置视图

### 8.6 视口与导航交互

- 画布支持：
  - 无限滚动
  - 网格背景
  - 迷你地图
  - 当前缩放比例显示

- 视口操作规则：
  - 滚轮默认缩放，缩放中心为光标位置
  - 触控板双指捏合缩放画布
  - 触控板双指拖拽平移画布
  - 中键按住拖拽平移画布
  - `Space + Drag` 进入临时平移
  - 双击迷你地图区域可快速跳转
  - `Fit to Screen` 用于缩放到当前图可见边界
  - 画布缩放行为不得触发浏览器页面缩放
  - 画布滚动行为不得触发页面整体滚动
  - `Cmd/Ctrl + + / - / 0` 在画布热区内应优先控制画布视口，而不是浏览器页面级缩放

- 拖拽期间的视口行为：
  - 当拖拽对象接近视口边缘时自动滚动画布
  - 自动滚动速度随距离边缘远近渐进变化
  - 拖拽时保留幽灵预览，避免目标对象跳动
  - 节点、子图或当前拖拽选区在放置时必须参与碰撞检测，若与其他节点、其他子图容器发生重叠，应优先调整当前被放置对象自身位置，而不是推开周围对象
  - 碰撞检测需同时覆盖：
    - 节点与节点
    - 节点与外部子图容器
    - 子图内容整体移动时与外部节点、外部子图的关系

### 8.6.1 Mac 触控板与手势规范

- 必须将 Mac 触控板视为一等输入设备，而不是鼠标兼容模式。
- 在 macOS 语境下，文档中所有 `Ctrl` 级交互默认都必须等价支持 `Command` 键；用户习惯上按下 `Command` 时应得到同样结果。
- 在 `画布模式` 中，系统应优先支持以下原生手势：
  - 单指点击 / 轻点：
    - 选择节点、边、子图或空白
  - 单指按下后拖动：
    - 在节点上为移动
    - 在空白上为框选
  - 双指滑动：
    - 平移画布
    - 禁止触发浏览器页面滚动
  - 双指捏合：
    - 以手势中心为锚点缩放画布
    - 禁止触发浏览器页面级缩放
  - 双指点击或双指按压：
    - 打开上下文菜单
  - 双指轻扫到边缘：
    - 不得误触浏览器前进 / 后退导航

- 浏览器级缩放隔离规则：
  - 当指针位于画布主区域内时，`pinch`、`Ctrl/Cmd + wheel`、`Ctrl/Cmd + +/-/0` 应优先作用于画布视口
  - 画布应主动阻止浏览器默认的页面缩放行为
  - 仅当焦点不在画布主区域时，浏览器默认缩放快捷键才可生效

- 触控板行为需满足以下规则：
  - 在指针位于画布上时，页面整体滚动应被拦截
  - 缩放和平移应具有惯性和平滑过渡，不允许顿挫式跳变
  - 触控板滚动和鼠标滚轮需要分别归一化处理，避免缩放速度失控
  - 触控板平移优先于框选，不应因为双指滑动误触对象选择
  - 当用户正在编辑文本时，触控板滚动应优先作用于编辑区，而不是画布

- 推荐的具体交互映射：
  - 空白画布上双指滑动：
    - 始终平移画布
  - 选中节点后双指滑动：
    - 仍然平移画布，不移动节点
  - Pinch In / Pinch Out：
    - 分别缩小 / 放大
    - 缩放时保持光标或手势中心附近对象尽量稳定
  - `Space` 按住后单指拖动：
    - 与双指平移语义一致，作为键盘辅助方案

- 画布容器在 Web 层应具备：
  - `overscroll-behavior` 抑制浏览器回弹与历史导航
  - 手势事件与 wheel 事件归一化
  - 对 Safari / Chrome 在 macOS 下的差异处理

- `源码模式` 中应尽量保留原生编辑器触控板习惯：
  - 双指滚动滚动文本
  - Pinch 不强行接管
  - 文本选择、光标定位与系统行为一致

### 8.6.2 输入设备矩阵

- PRD 需明确不同输入设备下的主操作映射，避免实现偏向某一种设备：

- 鼠标：
  - 左键点击选择
  - 左键拖动移动 / 框选
  - 中键或 `Space + Drag` 平移
  - 滚轮缩放或按配置平移
  - 右键菜单

- 触控板：
  - 单指选择 / 拖拽
  - 双指平移
  - Pinch 缩放
  - 双指右键

- 键盘：
  - 创建、删除、复制、分组、切模式、打开命令面板
  - 可在不触碰鼠标的情况下完成高频结构编辑

- 触摸屏若后续支持：
  - 单指拖动画布或对象
  - 双指平移 / 缩放
  - 长按触发上下文菜单

- 在需求和实现上应保证：
  - 不同输入设备之间语义一致
  - 快捷键与手势互为补充而不是彼此冲突
  - 无障碍场景下可只通过键盘完成主要编辑流程

### 8.6.3 安卓触摸与浏览器手势规范

- 安卓竖屏必须将触摸输入视为一等输入设备，不可沿用鼠标优先交互。
- 画布模式的基础手势语义：
  - 单指点按：
    - 选择节点、连线、分组或空白
  - 单指拖动：
    - 命中节点时移动节点或当前选区
    - 命中空白时平移画布
  - 长按空白：
    - 进入框选待命态或弹出轻量创建菜单
  - 长按节点：
    - 显示节点上下文动作
  - 双指拖动：
    - 始终平移画布
  - 双指捏合：
    - 始终缩放画布

- 安卓竖屏与桌面最大的差异要求：
  - 空白单指拖动默认优先平移，而不是立即框选
  - 框选必须通过长按空白后拖动、工具切换或专门入口触发
  - 这样可以避免用户在竖屏触摸设备上误触框选，保证主舞台导航更自然

- 安卓竖屏的画布触摸规则：
  - 命中节点后的拖动阈值应比桌面更高，避免轻触误拖
  - 节点拖动开始前建议存在 `80-140ms` 的轻微判定窗口
  - 单指拖动节点时，视口应可在接近边缘时自动滚动
  - 拖动中应显示清晰的选中轮廓和目标吸附预览

- 安卓竖屏的浏览器冲突抑制：
  - 在画布热区内应抑制页面整体滚动
  - 在画布热区内应抑制浏览器双击缩放
  - 在画布热区内应抑制地址栏导致的布局抖动
  - 需要正确处理 `100dvh`、安全区和浏览器工具栏伸缩

- 安卓竖屏的工具触达规则：
  - 高频操作应位于拇指可达的下半区
  - 低频操作应进入二级菜单、更多菜单或底部 Sheet
  - 需要精确点按的连接锚点在移动端必须放大命中区域
  - 连接锚点建议在选中节点后再显示，而不是长期占据节点四周

- 安卓竖屏的反馈规则：
  - 不使用说明型 notice
  - 使用轻量态反馈：
    - 选中描边
    - 吸附线
    - 拖拽幽灵
    - 保存状态点
    - Sheet 顶部标题与对象摘要
  - 删除、合并、重置等危险动作应提供可撤销路径
  - 底部属性 Sheet 在编辑输入框、数字字段或颜色字段时不得因遮罩层或焦点切换而自动收起

- 安卓竖屏的源码模式触摸规则：
  - 文本滚动、选择、光标移动优先遵循系统输入习惯
  - 代码输入时不接管浏览器缩放
  - 虚拟键盘弹出后，编辑器可视区必须自动抬升
  - 预览面板若为上拉 Sheet，展开和收起不得打断键盘焦点

- 安卓竖屏的协同 presence 规则：
  - 远端光标、视口和选区在移动端应弱化展示
  - 默认展示彩色头像点与对象边缘高亮
  - 点击协作者入口后再进入详细协作列表
  - 不允许远端 presence 长期压住画布中心区域

### 8.7 节点创建与编辑交互

- 创建节点的入口：
  - 双击空白创建孤立节点
  - 从节点锚点拖到空白创建并自动连线
  - 通过命令面板创建
  - 通过快捷键创建与当前节点相关的新节点

- 节点文本编辑规则：
  - 编辑对象是节点内容本身，而不是独立名称字段
  - 单击选中，双击或 `Enter` 进入编辑
  - 桌面端必须支持直接在节点内部原位编辑，不弹出单独的文本编辑悬浮窗
  - 编辑中直接按 `Enter` 提交当前节点内容
  - 编辑中 `Shift + Enter`、`Ctrl + Enter`、`Alt + Enter` 应插入换行，而不是提交
  - 节点文本框内必须保留常见编辑快捷键，如 `Ctrl/Cmd + A`
  - 所有文本编辑态，包括节点原位编辑、连线标签编辑、分组标题编辑、属性输入与源码编辑，都必须允许 `Ctrl/Cmd + A` 执行原生全选文字
  - 编辑中 `Esc` 放弃本次修改并回到选中状态
  - 点击空白处默认提交，除非文本语法非法
  - 编辑过程中节点尺寸需实时跟随内容变化，而不是只在提交后调整
  - 节点尺寸应根据内容自动扩展，保证文本始终被节点包住

- 节点移动规则：
  - 拖动阈值应避免误触编辑
  - 默认启用网格吸附
  - 按住临时绕过键时可暂时关闭吸附
  - 多选节点移动时保持相对位置
  - `Alt + 拖拽` 已选节点时，应先复制一份再进入拖动，复制内容应保留内部连线

- 节点复制与粘贴：
  - 复制后粘贴结果相对原位置偏移固定距离
  - 若复制内容包含内部连线，应一并复制
  - 跨文档粘贴时优先保持标准 Mermaid 结构

### 8.8 连线交互

- 连线创建规则：
  - 节点左右两侧应提供可点击或拖拽的连线手柄图标，作为主要连线入口
  - 从节点锚点拖出后进入 `Connecting`
  - 默认创建带箭头的标准方向边
  - 按住 `Ctrl` / `Cmd` 再从手柄点击或拖出时，创建普通无箭头连线
  - 连线从节点边缘锚点出发，并在离开节点后先保留一小段直线 stub，再进入转弯或曲线
  - 连线接入目标节点前也应保留一小段直线 stub，避免箭头贴边后被节点遮挡
  - 可连接目标高亮显示
  - 非法目标不高亮
  - 释放到合法目标则创建边
  - 释放到空白区域则创建新节点并自动连接
  - 释放到非法区域则取消连线

- 连线编辑规则：
  - 单击选中连线
  - 双击连线本体或边标签区域都应直接进入标签编辑
  - 连线标签应直接显示在连线中部附近，编辑时在画布原位进入轻量输入态
  - 连线标签的可见文字区域与背景胶囊都必须属于该连线的点击命中范围
  - 框选若未命中节点但命中了连线路径，应允许进入连线选中态
  - 连线被选中后按 `Delete` 只删除连接本身，不删除两端节点
  - `Delete` 删除已选连线
  - 方向切换通过属性面板、右键菜单或命令面板完成

- 连线避让规则：
  - 同一对节点之间的多条边，包括双向边，不应完全重叠
  - 系统应自动为平行边分配不同 lane offset，保持路径分离
  - 避让应尽量保持当前自动选边和自动换位的视觉逻辑，不得退化为僵硬直线
  - 双向边的两端锚点必须基于同一对节点的公共法线做稳定分离，保证：
    - 近端不会黏在一起
    - 中段不会像打结一样交叉缠绕
    - 左右、上下、斜向三类相对位置都能自适应

- 连线命中与样式规则：
  - 连线的实际点击命中区域应大于可见描边，避免细线难以点中
  - 默认连线描边必须在深色画布上保持足够可见度，不能细到只剩箭头清晰可见
  - 连线属性面板必须允许设置颜色和粗细值
  - 连线颜色与粗细在画布渲染、协同同步和 Mermaid 导出时都应保持一致

- 多重边策略：
  - 默认允许多重边，但在创建时提供重复连接提示
  - 若内容完全重复，可提示合并

- 自环策略：
  - V1 默认禁止自环
  - 若管理员或项目设置启用，则在兼容性提示中明确标记

### 8.9 子图与分组交互

- 子图是 V1 的标准分组方式，对应 Mermaid `subgraph`。
- 创建方式：
  - 多选后 `Ctrl/Cmd + G`
  - 右键菜单“创建子图”
  - 命令面板“Wrap in Subgraph”

- 子图交互规则：
  - 子图在画布上应表现为“窗口式分组容器”，而不是单纯虚线边框
  - 子图顶部必须有固定标题栏，标题栏承担拖拽手柄作用
  - 拖动子图标题栏时，应整体移动子图内的全部节点与嵌套子图内容
  - 双击子图标题可直接在标题栏原位重命名
  - 标题栏应提供至少折叠/展开、删除两个直接动作
  - 子图属性面板必须允许修改分组填充色、描边色和文字色
  - 子图属性面板应提供与节点一致的样式预设，允许一键切换分组风格
  - 子图与节点应尽量使用同一套视觉语言和交互心智，只保留“容器能包住内容”这一差异
  - 选中子图边框或标题栏可整体选中子图
  - `Ctrl/Cmd + 拖拽节点` 进入子图时必须显示明确的落组高亮预览
  - 松手后若命中目标子图，应直接把当前拖拽节点纳入该子图
  - `Ctrl/Cmd + 拖拽节点` 必须被视为“改层级模式”，松手后如果未命中任何子图，则节点应脱离当前子图并回到外层画布层级
  - 在 `Ctrl/Cmd + 拖拽节点` 的改层级模式中，子图边界框必须冻结为拖拽开始前的边界，不得因为预览节点移动而实时自适应收缩或扩张，否则会导致无法稳定从组内拖出到外层
  - `Ctrl/Cmd + 拖拽节点` 到另一个节点本体上时，目标节点应自动转换成 Mermaid `subgraph`，并以原节点标题与样式作为新分组容器
  - 节点转换成分组时，应尽量复用原节点 ID，以保持外部连线关系不被破坏
  - 若某个分组没有嵌套子图，且其唯一一个节点被拖出该分组，则该分组应自动收缩回一个普通节点
  - 分组收缩回节点时，也应尽量复用原分组 ID，以保持外部连线关系不被破坏
  - 分组拖拽也必须支持与节点相同的改层级模式，包括拖入其他分组、拖回外层，以及拖到节点后把节点转换为新的父分组
  - 在子图主体区域双击创建节点时，新节点必须直接归属当前命中的最内层子图
  - 双击子图主体空白区域时，优先在该子图内部创建新节点；只有双击标题本身时才进入标题编辑
  - 从节点拖出连线并在子图主体空白区域自动创建新节点时，新节点也必须直接归属当前命中的最内层子图
  - 拖拽节点移出子图时显示结构变化预览
  - 拖拽子图内部节点时，子图范围必须实时随内容刷新，而不是等待拖拽结束后再跳变
  - 折叠子图后，不允许退化成“全部断线 + 只剩空框”
  - 折叠子图后，应压缩成最小信息体积，并保留：
    - 分组标题
    - 内部节点名称摘要
    - 与外部对象的连线视觉
  - 折叠态下，内部节点对外连线应自动挂接到折叠后的分组容器上
  - 若内部连线两端都被折叠到同一个分组容器，则不应在外部重复画出无意义自环

- 子图视觉规则：
  - 视觉应统一为沉浸式工具风格下的“轻量窗口容器”
  - 标题栏、内容区、操作按钮、选中态、落组高亮必须属于同一套风格语言
  - 子图需要明显表达“容器”而非“装饰框”，用户应能一眼看出可拖动、可命名、可管理
  - 子图窗口必须作为内容底板存在，节点与连线始终位于其上层，不能被子图背景或窗口体遮挡
  - 子图颜色样式必须贯穿到：
    - 子图窗口本体
    - 子图标题栏
    - 子图作为连线起点时的默认出发色
    - Mermaid 导出后的 `style subgraphId ...` 标准语法
  - 子图窗口主体不得显示说明性文案、占位提示或教学文字，状态反馈应尽量通过边框、高亮、折叠状态和按钮本身表达
  - 折叠态视觉应是“最小信息卡”，而不是只剩标题条

- 嵌套子图规则：
  - 允许嵌套
  - 当前编辑焦点应清晰显示所在子图路径
  - `Shift + Tab` 可将节点移出当前子图层级

### 8.10 对齐、排列与布局交互

- 网格吸附默认开启，但可临时按键绕过。
- 提供三种布局状态：
  - `Auto`
  - `Hybrid`
  - `Pinned`

- `Auto`：
  - 主要依赖自动布局。
- `Hybrid`：
  - 先自动布局，再允许局部拖动修正。
- `Pinned`：
  - 主要以内嵌 compat layer 中的位置为准。

- 选区形成后支持：
  - 左对齐
  - 水平居中
  - 右对齐
  - 顶部对齐
  - 垂直居中
  - 底部对齐
  - 水平均分
  - 垂直均分

- 自动布局支持三个粒度：
  - 全图整理
  - 当前子图整理
  - 当前选区整理

- 执行自动布局前应提供预览或可撤销保障。
- 必须提供“一键整理”入口，用于在尊重当前相对结构关系的前提下压紧布局：
  - 应基于用户当前手工放置结果做小幅整理，而不是整体重排成另一套布局
  - 主要目标是收掉过大的空隙、减少局部碰撞风险、让结构更紧凑
  - 必须尽量保留原有层次、方向、相对邻近关系和用户心智地图
  - 保留可阅读留白
  - 不允许把节点挤到互相贴边
  - 不允许把连线标签压缩到难以辨认
  - 整理后仍可通过撤销立即回退

### 8.11 源码模式交互

- 不常驻，不与画布同屏。
- 进入源码模式后，以完整编辑器体验为主。
- 支持：
  - Mermaid 语法高亮
  - Markdown 高亮
  - Mermaid block 导航
  - 错误定位
  - 一键回到画布并定位选中对象

- 进入源码模式时：
  - 默认定位到当前 Mermaid block
  - 若画布中已有选中对象，应尽量定位到对应语句附近
  - 安卓竖屏下默认进入单列全屏编辑器，不与预览强行并排
  - 安卓竖屏下预览应通过上拉 Sheet 或单独全屏页打开
  - 安卓竖屏下虚拟键盘弹出后，工具区应自动收敛，避免遮挡输入区

- 保存与切换规则：
  - 在源码模式中允许临时保留未通过解析的草稿
  - 源码输入过程中只更新草稿与预览，不得每输入一个字符就立即保存到共享图模型
  - 源码草稿应在失焦、退出源码模式或显式确认时再尝试提交
  - 当用户尝试返回画布时：
    - 若语法有效，则更新画布
    - 若语法无效，则保留在源码模式并提示错误
    - 用户可选择继续修复、放弃草稿或回退到上一个有效版本

- 源码到画布的映射规则：
  - 若仅文本内容变化，则尽量保留现有布局
  - 若结构变化但节点 ID 可匹配，则只对新增或失配部分重新布局
  - 若节点 ID 大范围重写，则提示可能丢失现有工作区布局

### 8.12 协同交互

- 画布模式下显示：
  - 在线成员列表
  - 实时光标
  - 实时选区轮廓
  - 当前正在编辑的节点或子图标识
  - 当前谁正在查看当前文件
  - 当前谁正在编辑当前 Mermaid block
  - 当前谁正在浏览当前视口区域

- 协同编辑规则：
  - 多名用户可同时打开同一个云端文件并实时同步
  - 节点文本编辑采用软锁提示，而不是强制硬锁
  - 若其他人正在编辑同一节点，当前用户仍可进入，但需收到冲突提示
  - 节点、连线、子图、布局和评论变更需广播并实时更新其他人视图
  - 远端用户的选择框、正在拖拽状态和正在编辑标签应具备颜色区分
  - 用户可点击某个成员头像并选择“跟随其视图”

- 源码模式协同规则：
  - V1 建议采用 Mermaid block 级软锁
  - 当一名用户进入某 block 的源码编辑时，其他用户看到“正在编辑源码”的提示
  - 其他用户可只读查看、等待释放，或申请接管
  - 若多人同时进入源码模式，系统需至少提供块级冲突检测和版本提示

- 评论交互：
  - 评论可锚定在节点、连线、子图或文档级
  - Hover 或选中对象时显示评论入口
  - 评论线程支持已解决状态

- 资源管理器中的协同可见性：
  - 文件列表显示在线人数
  - 文件列表显示最近编辑者
  - 文件夹与文件项显示未读评论和冲突标识
  - 文件正在被多人编辑时显示 presence 徽标

### 8.12.1 多人共编一致性细则

- 本产品默认支持“多人同时编辑同一个云端文件，同步看到彼此改动”的核心协作模式。
- 一致性规则建议为：
  - 结构操作采用操作流同步
  - 文本编辑采用 block 级协同策略
  - 画布布局采用内嵌 compat layer 协同流
  - 评论与 presence 独立于正文同步

- 实时协同至少需要广播以下事件：
  - 用户进入 / 离开文件
  - 当前视口变化
  - 选择变化
  - 节点 / 边 / 子图结构变更
  - 节点文本编辑开始 / 结束
  - 源码模式进入 / 退出
  - 评论新增 / 解决

- 冲突处理需按对象类型区分：
  - 节点文本：
    - 软锁提示 + 合并或覆盖提示
  - 结构变更：
    - 以操作流顺序合并，并可撤销回放
  - 布局变更：
    - 优先保留最新确认位置，但应允许恢复个人视图
  - 源码模式：
    - 以 Mermaid block 为最小冲突单位

- Presence 显示建议：
  - 文件级：谁正在打开
  - block 级：谁正在编辑当前图
  - 对象级：谁正在编辑当前节点 / 子图 / 边
  - 视口级：谁正在看哪里

- 用户应能看到：
  - 远端光标颜色与姓名
  - 远端选区边框
  - 远端正在输入或拖拽中的状态
  - 一键跟随某个协作者视角

- 本地体验需保持乐观更新：
  - 用户操作先在本地即时反映
  - 协同层异步确认
  - 若服务端拒绝或冲突失败，则给出回退提示

### 8.13 扩展接口预留

- 当前版本提供一个最小 AI 验证入口：
  - AI 入口应与右侧属性面板合并为同一超页面
  - 右侧面板顶部提供 `属性 / AI` 页签切换
  - AI 面板只包含两个主界面：`聊天` 与 `设置`
  - 该入口用于快速验证上下文读取、接口联通与后续 Tool API 接入
  - AI 面板内部的滚轮、触控板双指滑动、触摸滑动只作用于 AI 面板自身，不得透传给画布平移或缩放
- AI 锁定开关不应放在设置页，而应放在聊天输入区上方，作为 `Agent 模式控制` 的即时开关：
  - `锁定简介与标题`
  - `锁定附加信息`
  - `锁定节点注释`
  - `锁定流程图`
- 聊天发送前用户必须能直接看到当前锁定状态，不需要切到设置页确认。
- 默认值：
  - `锁定简介与标题`：开
  - `锁定附加信息`：开
  - `锁定节点注释`：开
  - `锁定流程图`：关
- 锁定规则不仅写入提示词，也必须在 Tool API 写入阶段真实生效，避免 AI 越权改写。
- 设置页只保留模型、API、本地密钥和系统提示词等配置项，不承担实时 Agent 行为控制。
- 最小 AI 验证入口暂不视为最终产品形态，不代表未来正式工作台的 UI 方案。
- 后续若扩展智能能力，仍必须优先通过统一 SDK / Tool API 进入，而不是继续堆叠零散 UI。
- 任何未来的外部工具结果都应先进入预览、diff 或命令批次，再由用户确认。

### 8.14 快捷键与效率交互

- 必须定义并文档化高频快捷键：
  - 选择全部
  - 复制 / 粘贴 / 重复
  - 删除
  - 撤销 / 重做
  - 创建子图
  - 自动布局
  - 切换源码模式
  - 搜索与命令面板

- 命令面板是高频入口，需支持：
  - 模糊搜索命令
  - 最近操作
  - 布局命令
  - 导入导出命令

- V1 默认快捷键至少包括：
  - `Ctrl/Cmd + A`：全选当前画布可见节点
  - `Ctrl/Cmd + Z`：撤销
  - `Ctrl/Cmd + Shift + Z`：重做
  - `Cmd + Ctrl + Z`：macOS 兼容重做
  - `Alt + 拖拽`：复制并拖动当前选中节点

- 撤销 / 重做规则：
  - 只要用户当前不在文本输入态，`Ctrl/Cmd + Z` 与 `Ctrl/Cmd + Shift + Z` 都应作为应用级撤销 / 重做生效，不应要求画布必须重新获得焦点
  - 文本输入态，包括节点原位编辑、连线标签、分组标题、属性文本输入与源码编辑，必须优先保留浏览器 / 编辑器原生撤销行为
  - 非文本输入控件，如颜色选择、复选框、下拉选择、面板按钮，不应吞掉应用级撤销 / 重做
  - 撤销 / 重做行为在画布、属性栏、文件栏、模式切换栏等工作台区域应保持一致，不得出现“点了侧栏之后撤销失效”的情况

- 剪贴板规则：
  - 支持复制为 Mermaid 文本
  - 支持复制为图片
  - 支持跨文档粘贴结构

### 8.15 反馈、错误与空状态

- 所有关键操作都应有即时反馈：
  - 保存成功
  - 导出成功
  - 同步失败
  - 兼容性警告
  - 协同冲突提示

- 错误提示分层：
  - 不使用说明型 notice 或 toast
  - 结构错误使用面板提示
  - 阻断型错误使用弹窗

- 空状态需覆盖：
  - 新文档空画布
  - 导入失败
  - compat layer 缺失
  - 当前无评论
  - 当前无搜索结果

- 首次使用时需提供轻量引导：
  - 如何创建节点
  - 如何拖拽连线
  - 如何进入源码模式
  - 如何创建子图

### 8.16 搜索、定位与历史交互

- 搜索面板支持搜索：
  - 节点文本
  - 节点 ID
  - 子图标题
  - 评论内容
  - Mermaid block

- 搜索结果交互：
  - 单击结果定位到画布对象
  - 键盘上下切换结果
  - 在画布中高亮搜索命中
  - 若对象在折叠子图中，应自动展开并定位

- 历史模式交互需支持：
  - 时间线浏览
  - 按用户筛选
  - 按操作类型筛选
  - 查看某次变更影响的节点、连线、子图

- 版本对比应至少提供：
  - Markdown / Mermaid 内容差异
  - 结构差异摘要
  - 布局变更摘要

- 恢复规则：
  - 支持恢复整个文档版本
  - 支持将旧版本复制为新分支草稿
  - 恢复前需提示覆盖范围

### 8.17 导入导出、保存与权限交互

- 导入交互支持：
  - 文件选择器导入
  - 拖拽文件到画布导入
  - 粘贴 Mermaid 文本导入

- 导入处理流程：
  - 自动识别 Markdown 中的 Mermaid block
  - 若存在 `lths-compat` block，则尝试自动关联兼容层信息
  - 若文档不是当前标准工程格式，则允许用户一键标准化：
    - 提取第一个可解析的 Mermaid block 作为结构层
    - 提取标题与 Summary 进入结构头部
    - 其余 Markdown 原文保留在 `Content` 层
    - 文末写入新的最短 compat layer
  - 若语法不兼容，则展示兼容性报告与修复建议
  - 若一个文档中存在多个 Mermaid block，则允许用户选择目标 block

- 导出交互支持：
  - 导出标准 Markdown
  - 导出 SVG / PNG / PDF
  - 导出包含 compat layer 的单文件工程

- 导出前应允许配置：
  - 导出范围：当前选区 / 当前 block / 整个文档
  - 背景：透明或纯色
  - 页面尺寸与缩放

- 保存策略：
  - 默认自动保存到云端项目文件
  - 顶部栏显示保存中、已保存、保存失败、同步延迟状态
  - 云端文件模式下，本地保留待同步草稿与恢复副本
  - 本地项目模式下，保存直接写回本地文件
  - 网络异常时保留本地草稿并提示稍后重试
  - 网络恢复后自动尝试与云端版本合并
  - 安卓竖屏下保存状态应压缩为顶部小型状态点或胶囊，不得使用占满横向空间的大横幅
  - 若本地草稿与云端版本冲突，应提供恢复、比较与另存为分支草稿能力

- 云端文件打开规则：
  - 用户进入项目后从资源管理器打开云端文件
  - 打开文件时先拉取云端最新 Markdown、compat layer、评论与版本信息
  - 若本地存在未同步缓存，需在打开时提示恢复策略
  - 文件关闭后保留最近工作缓存，便于快速恢复

- 权限交互：
  - 只读用户可查看、搜索、导出
  - 可评论用户可新增评论但不能修改结构
  - 可编辑用户可执行画布和源码修改
  - 管理员可管理成员、权限和版本恢复

- 只读态反馈：
  - 画布仍可导航与选择
  - 所有写操作入口禁用并显示原因

### 8.18 项目文件生命周期与资源操作

- 项目文件应遵循清晰的生命周期：
  - 创建
  - 命名
  - 移动
  - 重命名
  - 分享
  - 归档
  - 恢复
  - 删除

- 文件夹与文件操作需支持：
  - 拖拽移动
  - 批量移动
  - 批量归档
  - 复制链接
  - 复制项目内路径
  - 查看最近编辑记录

- 删除策略建议：
  - 默认进入回收站或归档区，而不是立即物理删除
  - 被多人引用或协同中的文件删除前需二次确认

- Mermaid block 级别也应具备资源操作：
  - 重命名 block 标题
  - 调整 block 顺序
  - 拆分 block
  - 合并 block
  - 从文件级目录中快速定位

## 9. 功能需求

### 9.1 节点

- 创建、编辑、删除节点
- 支持标准 flowchart 节点形状
- 支持节点文本编辑
- 支持链接
- 支持 class / style 映射
- 支持节点折叠与展开作为工作区状态
- 复制节点时只生成新的内部 ID，不应自动在用户可见内容中附加“副本”等后缀

### 9.2 连线

- 从源节点连接到目标节点
- 支持标准箭头类型与标准标签文本
- 支持方向切换
- 支持边样式映射到标准 Mermaid 可表达范围
- 禁止自环作为默认行为，可在设置中放开
- 连线视觉必须满足以下规则：
  - 默认使用自然贝赛尔曲线
  - 节点出入口两端均需带自然弯曲延伸，不使用僵硬的直线桩段
  - 箭头尺寸必须明显大于默认线宽，保证方向一眼可见
  - 在未手动指定连线颜色时，连线主色应默认继承源节点或源分组的主描边色，形成明确的“从谁发出”视觉延续
  - 连线应使用白色描边 + 半透明主色描边的双层视觉，保证交叉时仍能区分路径归属
  - 默认线宽应以当前基准减半后的细线为准，但不能细到在深色画布上难以辨认
  - 连线端点应根据方向自动避让，减少重叠与汇点堆叠
  - 双向互连的两条边在中段应额外拉开，避免视觉上贴成一束
  - 对于互为反向的两条边，应在不改变起点锚点与终点锚点的前提下，增强中段曲率分离，让两条线在主体路径上保持更明显距离
  - 互为反向的两条边不应被弯到同一侧形成“桥状”重叠，而应自然落在连接轴线两侧，同时保持较平顺的中段曲率，不应出现过高拱起
  - 互为反向的两条边在端点附近也必须沿同一世界法线做稳定分离，避免在上下、左右或斜向场景中出现近端打结、缠绕或交叉
  - 连线标签默认不放在绝对中点，而应更靠近目标端，减少双向标签互相黏连
  - 连线标签的几何中心应与其标签锚点对齐，不得出现整体上下漂移导致“中心不在线上”的视觉偏差
  - 连线标签必须带有对应色系的浅色背景底和清晰描边，默认优先采用浅底深字，避免文字与连线或深色画布糊在一起
  - 源节点或源分组继承来的主色必须在主线路径与箭头上清晰可见，不得被白色描边掩盖到接近无色
  - 即使数据层线宽为默认细线，工作区渲染层也应保证一个最低可见视觉线宽，避免在深色画布上显得过细
  - 只要连线一端或两端连接的是分组容器，该连线在工作区视图中应自动增强表达：
    - 线宽约为普通连线的两倍
    - 使用独立色系，与普通节点连线明显区分
    - 标签颜色也应跟随这一套区分色

### 9.3 子图

- 多选创建子图
- 拖拽节点进出子图
- 子图标题编辑
- 子图折叠与展开
- 子图嵌套
- 子图必须可作为标准连线端点参与连接：
  - 子图到节点
  - 节点到子图
  - 子图到子图
- 子图的连线交互应与节点复用同一套逻辑：
  - 可从左右端拖出
  - 可接收拖入连线
  - 可被选中、编辑标签、删除连线

### 9.4 自动布局

- 支持整图自动布局
- 支持对子图内局部自动布局
- 支持按方向切换 `TD/LR/RL/BT`
- 支持“整理当前选区”
- 工作台必须同时提供两个不同目标的动作：
  - `整理`
    - 基于当前用户摆放做增量优化
    - 保留当前方位、簇关系和大致层级
  - `布局`
    - 明确允许忽略当前手工摆放
    - 直接按拓扑结构重排节点和子图
- “整理”默认应为温和整理，不应在未明确要求下把整图大幅改位
- “整理”应优先尊重用户现有编排：
  - 只做局部压缩、间距修正和轻微避让
  - 保留原有相对顺序、主要方向和视觉分组
  - 单次整理应限制最大位移，避免把用户手工编排推翻
- “布局”应优先尊重语义图，而不是当前坐标：
  - 按连接关系寻找更清晰的拓扑层级
  - 尽可能减少打结、逆向遮挡和线标签冲突
  - 在不重叠的前提下尽量压紧空间

### 9.4.1 温和整理算法设计

- 整理功能不应采用“整图重新自动布局”的单阶段算法。
- 推荐采用 `保方位增量整理算法`：
  - 第一阶段：骨架抽取
  - 第二阶段：局部压缩
  - 第三阶段：碰撞修正
  - 第四阶段：理线与标签避让

- 核心目标函数必须同时满足：
  - 保留用户当前心智地图
  - 收掉多余空白
  - 减少节点、子图、附加信息卡之间的碰撞
  - 减少多重边、反向边、汇聚边的缠绕
  - 保留连线标签可读性

- 第一阶段：骨架抽取
  - 读取当前布局中的节点中心点、子图容器、附加信息卡位置。
  - 根据当前布局而不是语义图重新推断：
    - 主方向
    - 层次顺序
    - 相邻关系
    - 视觉簇
  - 必须记录每个对象相对邻居的：
    - 左右关系
    - 上下关系
    - 所属子图
    - 与最近关键邻居的相对距离
  - 若对象被用户明确 Pin、最近手动拖动过、当前被选中、或属于折叠子图，则应提高其权重，整理时优先少动。

- 第二阶段：局部压缩
  - 按当前主方向做 `sweep compaction`：
    - 若主方向为 LR / RL，则先压缩横向，再微调纵向
    - 若主方向为 TD / BT，则先压缩纵向，再微调横向
  - 压缩时不得改变当前对象相对顺序。
  - 压缩的本质不是“往一个点吸附”，而是：
    - 保留最小安全间距
    - 收掉超出目标留白的空隙
    - 保留局部簇内部的相对结构
  - 最小安全间距应同时考虑：
    - 节点自身宽高
    - 连线标签尺寸
    - 子图标题栏
    - 附加信息卡
  - 每一轮压缩都必须限制单对象最大位移，避免出现整体跳位。

- 第三阶段：碰撞修正
  - 在压缩结果上，对节点、子图、附加信息卡建立包围盒碰撞体积。
  - 若发生碰撞，应优先移动“当前整理中被推导出来的对象”，而不是反推周围对象。
  - 解决碰撞时应采用最小位移原则：
    - 优先沿主方向轻微平移
    - 不够时再沿副方向让位
    - 不允许跨越已经确定的顺序边界
  - 子图内部节点碰撞只在子图内部解，不得把节点推出所属子图。
  - 子图之间的碰撞修正必须保留容器边距与标题栏安全区。

- 第四阶段：理线与标签避让
  - 理线必须基于整理后的节点结果重新计算，但不能强行改变端点归属。
  - 推荐采用 `端点法线分离 + 中段走廊路由`：
    - 多重边按 lane 分配
    - 反向边稳定分居连接轴线两侧
    - 汇聚边在端点附近按角度自动错位
    - 近端先做小尺度法线偏移，再进入中段曲线或走廊
  - 对于同一节点上的多个入边或出边：
    - 应根据角度排序后分配端点顺序
    - 避免所有线都汇到同一个视觉点上
  - 连线标签必须参加避让：
    - 与节点
    - 与子图边框
    - 与其他连线标签
    - 与附加信息卡
  - 双向边标签应默认靠近各自输入端，避免贴在一起。

- 推荐的打分函数可由以下代价组成：
  - `orderPenalty`
    - 若整理后改变了用户原本的左右或上下顺序，则高惩罚
  - `displacementPenalty`
    - 节点位移越大惩罚越高
  - `gapPenalty`
    - 超大空白会被惩罚
  - `overlapPenalty`
    - 节点、子图、标签碰撞高惩罚
  - `crossingPenalty`
    - 线交叉、端点缠绕高惩罚
  - `labelPenalty`
    - 标签难读或重叠高惩罚
  - 权重建议：
    - 顺序保持 > 碰撞消除 > 标签可读 > 线交叉减少 > 空间压缩

- 推荐实现策略：
  - 不要直接套用 Dagre / ELK 的完整重布局结果作为整理结果。
  - 更适合在现有自由布局基础上做：
    - `constraint-based incremental solver`
    - 配合 `iterative local relaxation`
  - 每轮最多执行少量迭代，保证可预测、可撤销、响应快。

- 推荐的整理模式：
  - `Soft Tidy`
    - 默认模式
    - 只做温和压缩与局部理线
  - `Selection Tidy`
    - 只整理当前选区及其相邻连线
  - `Subgraph Tidy`
    - 只整理当前子图内部
  - `Reflow`
    - 明确允许更大改动的重排模式

### 9.4.2 拓扑布局算法设计

- `布局` 功能应独立于 `整理`，不能只是在整理上加大位移阈值。
- 推荐采用 `拓扑分层 + 稳定排序 + 密集打包`：
  - 第一阶段：语义边抽取
    - 以节点和子图之间的连接关系为主
    - 子图若作为连线端点，应映射到其内部代表节点或代表簇
  - 第二阶段：强连通分量压缩
    - 对环路先做 SCC 压缩，避免简单拓扑排序在反馈边上失效
  - 第三阶段：分层
    - 依据方向 `TD/LR/RL/BT` 生成层级 rank
    - 隔离节点可作为附加层或边缘层处理，不得全部堆叠在同一列
  - 第四阶段：层内排序
    - 用相邻层重心排序、当前层 barycenter、子图归属做稳定排序
    - 目标是减少线交叉而不是只按 ID 排列
  - 第五阶段：密集打包
    - 按节点尺寸、标签尺寸、子图边距打包
    - 保证不遮挡的前提下尽量压缩空间
  - 第六阶段：理线
    - 重算多重边 lane、反向边分侧、汇聚边端点顺序和标签位置
- `布局` 执行结果允许明显改变原始坐标，但仍需：
  - 保证可撤销
  - 保留子图归属
  - 保留附加信息卡固定策略
  - 在同一语义结构下具有稳定性，避免每次布局都完全不同

- 推荐的工程实现顺序：
  - 先实现：
    - 局部压缩
    - 碰撞修正
    - 双向边与多重边 lane 分离
  - 再实现：
    - 标签避让
    - 汇聚边端点排序
    - 选区整理
  - 最后再实现：
    - 全图多轮评分优化
    - 更复杂的走廊路由

### 9.5 导入导出

- 导入：
  - `.md`
  - `.mmd`
  - 粘贴 Mermaid 代码

- 导出：
  - 标准 Markdown
  - SVG
  - PNG
  - PDF

- 工作区保存：
  - 单个 Markdown 工程文件
  - 文末内嵌 compat layer

### 9.6 协同

- 多人同时编辑
- 实时光标和选区显示
- 评论与评论锚点
- 历史版本与回滚
- 文档锁定与冲突提示
- 只读、可评论、可编辑、管理员权限

### 9.7 项目与资源管理

- 项目列表
- 文件夹树
- 云端 Markdown 文件管理
- 本地项目文件管理
- 最近文件、收藏、分享
- 文件 presence 状态
- 文件权限与版本入口

### 9.8 扩展接口预留

- 当前版本在工作台中提供最小 AI 验证界面：
  - 右侧大面板中的 `AI` 页签入口
  - `聊天 / 设置` 两个页签
- 后续扩展能力应通过 SDK、命令系统和 Tool API 统一接入。
- 外部工具不能绕过权限系统直接写入最终文档。
- 外部工具产生的改动必须以命令或建议形式进入编辑器，再由用户确认。

### 9.9 工作台与资源导航

- 多文件标签管理
- 资源管理器展开 / 折叠
- 最近文件与收藏
- 项目切换
- 文件级 presence
- block 级跳转

## 10. 同步策略

### 10.1 核心原则

- Markdown/Mermaid 是内容真相源。
- compat layer 是工作区真相源。
- 云端项目文件是持久化真相源。
- 协同状态是会话真相源。
- 本地项目模式下，本地文件本身也是可编辑真相源。

### 10.2 图形到源码

- 节点、边、子图的结构性修改应立即同步到 Mermaid AST，再回写文本。
- 布局修改只更新 compat layer，不改写 Mermaid 内容。

### 10.3 源码到图形

- 源码模式保存或退出时重新解析 Mermaid。
- 若语义结构未变，则尽量保留现有布局。
- 若节点 ID 变化或结构重构严重，则触发布局恢复策略：
  - 保留可匹配节点的位置
  - 对新增节点自动布局
  - 对失配节点提示用户确认

### 10.4 云端同步与本地项目

- 打开文件时：
  - 拉取云端最新文档版本
  - 拉取 compat layer
  - 拉取协同会话状态
  - 检查本地是否存在待恢复草稿或对应本地项目副本

- 编辑过程中：
  - 本地立即更新画布与临时状态
  - 结构操作通过协同层广播
  - 云端持久化异步确认

- 断线时：
  - 允许继续在本地草稿或本地项目上工作
  - 清晰显示“离线草稿”状态
  - 恢复连接后执行差异合并或冲突处理

### 10.5 协同一致性与恢复策略

- 需区分四类同步流：
  - 文档正文流
  - 结构操作流
  - 布局 compat layer 流
  - presence / 评论流

- 服务端恢复策略建议：
  - 会话短断线时保留 presence 短暂存活
  - 长断线后重新加入协同房间并重放最近变更
  - 若客户端版本过旧，强制重新拉取最新快照

- 客户端恢复策略建议：
  - 重连后先获取最新服务端快照
  - 再比对本地未提交缓存
  - 再执行自动合并或提示人工选择

## 11. 系统架构

### 11.1 部署架构

- Linux 服务器提供：
  - Web 应用静态资源
  - API 服务
  - 协同服务
  - 项目与文件服务
  - 文档存储
  - 版本与审计

- 浏览器客户端负责：
  - 渲染画布
  - 节点与连线交互
  - 本地布局计算
  - 预览与导出渲染

- 若部署目标为 RK3366 这类低性能 ARM 开发板：
  - 服务器职责应尽量收缩为静态资源分发、文件读写、鉴权与协同转发
  - Mermaid 预览、画布渲染、自动布局、导出渲染尽量保持在客户端执行
  - 不应把图形计算、批量渲染和大文件导出堆到该服务器上长期运行

### 11.2 服务拆分建议

- `web-app`
  - 前端页面与静态资源
- `api-server`
  - 文档、权限、项目、评论、版本接口
- `project-service`
  - 项目、文件夹、文件树、最近文件、收藏、分享
- `realtime-server`
  - 协同同步、在线状态、光标广播
  - 基于 OT / CRDT 或等价操作流的实时合并
- `storage`
  - Markdown 工程文件存储
- `worker`
  - 异步导出、批处理、索引构建

### 11.3 存储建议

- 元数据：PostgreSQL
- 文件对象：S3 兼容存储或服务器文件存储
- 协同状态：Redis 或专用实时层
- 审计日志：独立日志表或日志管道
- 浏览器本地缓存：IndexedDB 或等价缓存层

- RK3366 部署建议：
  - 正式环境优先使用预构建静态产物 + `nginx`
  - 构建过程建议在更强的 CI 或开发机完成
  - 开发服务器与预览服务器不作为正式生产方案

## 12. SDK 与接口设计

### 12.1 设计目标

- 先定义 SDK，再逐步开放插件能力。
- SDK 优先服务内部自动化脚本、受控工具调用和后续插件开发。
- 所有扩展都基于统一命令与 Graph IR，而不是直接操作 UI 组件。

### 12.2 内部 Graph IR

- 统一抽象：
  - `Project`
  - `Folder`
  - `FileEntry`
  - `Document`
  - `MermaidBlock`
  - `Node`
  - `Edge`
  - `Subgraph`
  - `LayoutState`
  - `Selection`
  - `Command`

- 要求：
  - 能从 Mermaid 文本解析而来
  - 能回写为标准 Mermaid
  - 能与内嵌 compat layer 做稳定映射

### 12.3 SDK 能力边界

- 文档读取
- 图结构遍历
- 结构变换
- 命令执行
- 选择区读取
- 面板扩展
- 工具栏扩展
- 导入导出扩展
- 外部工具注册

### 12.3.1 接口分层建议

- 对外接口应分为两层，而不是只暴露整份 Markdown：
  - 语义图接口：
    - 面向 AI、自动化、审查、批处理
    - 默认不返回坐标、颜色、尺寸、视口等工作区噪音
    - 只返回 `Project Name / Summary / Content / Nodes / Edges / Subgraphs`
  - 原始文档接口：
    - 面向完整导入导出、源码模式、特殊修复和高级编辑
    - 返回完整 `.md` 原文
- AI 默认应优先走语义图接口；完整 Markdown 读写只作为兜底能力。
- 不建议让 AI 主要通过“整份 Markdown 直接改写”来编辑结构图，因为：
  - 容易误伤用户自由内容层
  - 容易破坏标准章节结构
  - 很难做局部审计、局部撤销和协同合并

### 12.3.2 AI 语义图读取接口

- 建议至少提供一个稳定的只读快照接口：
  - `getGraphSemanticSnapshot`
- 返回内容建议包括：
  - `project`
    - `name`
    - `summary`
    - `content`
    - `nodeAnnotations`
  - `diagram`
    - `direction`
    - `nodes[]`
      - `id`
      - `label`
      - `subgraphId`
      - `annotation`
    - `edges[]`
      - `id`
      - `from`
      - `to`
      - `label`
      - `type`
    - `subgraphs[]`
      - `id`
      - `title`
      - `parentId`
- 默认不返回：
  - `x / y / width / height`
  - `fill / stroke / textColor`
  - 视口与 compat layer 明细
- 该接口的目标是让 AI 用最少 token 理解图结构和文档主语义。

### 12.3.3 AI 语义图编辑接口

- 不建议把“AI IDE 风格自由文本改写”作为主编辑接口。
- 推荐主接口为结构化操作批次：
  - `applyGraphOperationBatch`
- 操作建议包括：
  - `createNode`
  - `updateNodeLabel`
  - `updateNodeAnnotation`
  - `deleteNode`
  - `createEdge`
  - `updateEdgeLabel`
  - `deleteEdge`
  - `createSubgraph`
  - `updateSubgraphTitle`
  - `moveNodeToSubgraph`
  - `updateProjectMeta`
  - `updateContentMarkdown`
- 这样做的好处：
  - 更容易校验
  - 更容易审计
  - 更容易撤销 / 重做
  - 更容易协同广播
  - 更容易限制 AI 的写入边界
- 若后续仍希望支持“像 AI IDE 一样编辑”，建议作为附加接口：
  - `applyGraphSemanticPatch`
  - 输入对象应是“语义快照 patch / diff”，而不是整份 `.md` diff

### 12.3.4 原始文档接口

- 为兜底和高级场景保留原始文档读写接口：
  - `getProjectMarkdown`
  - `setProjectMarkdown`
- `setProjectMarkdown` 必须内置：
  - 标准化结构校验
  - Mermaid 语法校验
  - `Node Annotations` 章节归一化
  - compat layer 重新归一化
  - 失败回滚
- 若输入不是当前标准单文件工程格式，应允许：
  - 返回标准化预览
  - 或直接执行标准化后再落盘

### 12.3.5 版本与并发控制

- 所有读写接口都应带上文档版本信息，例如：
  - `revision`
  - `updatedAt`
- 所有写接口都应支持：
  - `dryRun`
  - `validateOnly`
  - `expectedRevision`
- 这样可以避免：
  - AI 覆盖掉用户刚改完的内容
  - 协同场景下出现静默覆盖
  - 工具调用难以审计

### 12.4 命令系统

- 所有修改都应通过命令总线执行。
- 命令需具备：
  - 可撤销
  - 可重做
  - 可审计
  - 可协同广播
  - 可被自动化或外部工具链调用

- 示例命令：
  - `createNode`
  - `updateNodeText`
  - `connectNodes`
  - `createSubgraph`
  - `moveSelection`
  - `applyAutoLayout`
  - `rewriteMermaidBlock`

### 12.5 Tool API 预留

- 为后续外部工具提供受控工具接口，而不是直接提供数据库写权限。
- 工具建议包括：
  - `getGraphSemanticSnapshot`
  - `applyGraphOperationBatch`
  - `getProjectMarkdown`
  - `setProjectMarkdown`
  - `getSelection`
  - `runLayout`
  - `validateMermaidCompatibility`
  - `applyCommandBatch`

### 12.5.1 最小 AI 验证接口

- 为了快速验证 AI 接入，当前版本至少应提供以下 4 个接口：
  - `getGraphSemanticSnapshot`
    - 返回节点、连线、分组、Project Name、Summary、附加信息、节点注释
    - 默认不返回坐标、颜色、尺寸和 compat 细节
  - `applyGraphOperationBatch`
    - 允许结构化批量编辑图语义内容
    - 用于 AI 和自动化工具调用
  - `getProjectMarkdown`
    - 返回完整单文件工程 Markdown
  - `setProjectMarkdown`
    - 允许写回完整单文件工程 Markdown
    - 写入时必须重新解析并归一化为当前标准工程结构
- AI 验证聊天默认应在每次请求时自动附带：
  - `AI rules`
  - `Tool rules`
  - 当前单文件工程结构说明
  - `Conversation history`
  - 当前完整 Markdown
  - 相对上一轮 AI 对话的 Markdown 改动
  - 当前选中内容
  - 当前用户输入
- 当前完整 Markdown 在单次请求上下文中只保留最新版本，不回灌旧版完整文件。
- AI 若需读取结构语义，应优先读取 `getGraphSemanticSnapshot`，而不是反复展开旧版 Markdown。
- AI 聊天应允许直接调用本地 Tool API 修改工程：
  - `getGraphSemanticSnapshot`
  - `applyGraphOperationBatch`
  - `getProjectMarkdown`
  - `setProjectMarkdown`
- AI 工具调用应遵循：
  - 优先使用 `applyGraphOperationBatch`
  - 节点注释优先使用 `updateNodeAnnotation`
  - 当前选中对象优先视为本轮改动范围
  - 尽量保留 `Project Name / Summary / Diagram / Content / Node Annotations / lths-compat` 章节结构
  - 仅在结构化操作不足以表达目标时，才使用 `setProjectMarkdown`

- 所有外部工具调用必须经过：
  - 权限校验
  - 速率限制
  - 审计记录
  - 用户确认或策略确认
- AI 的 API URL、API Key、模型设置只能保存在用户本地环境中：
  - 不写入工程 Markdown
  - 不写入 compat layer
  - 工程文件复制给他人时，不应带出任何本地 AI 凭据

### 12.5.2 节点注释设计建议

- 目标是为节点增加“注释 / 说明 / AI 可读补充信息”，同时尽量保持标准 Mermaid 兼容。
- Mermaid 官方 flowchart 本身没有“节点私有注释字段”的标准语法。
- Mermaid 中现有接近能力主要有：
  - 节点标签文本
  - `%%` 注释
  - `click` / `classDef` / `style`
- 其中：
  - 节点标签是可见语义内容，不适合放隐藏注释
  - `%%` 是文档级注释，不是稳定的节点级结构字段
  - `style` / `classDef` 不是语义注释
- 若兼容标准 Mermaid 是最高优先级，不建议发明直接嵌入节点行的新私有语法，否则：
  - 会破坏其他 Mermaid 编辑器兼容
  - 会让 AI 误把私有语法理解为标准结构
  - 会增加解析与维护成本
- 最终推荐方案：
  - 标准 Mermaid 层只保留正常节点标签
  - 节点注释使用标准 Markdown 的独立章节：`## Node Annotations`
  - 每个节点注释条目使用三级标题绑定节点 ID：
    - `### \`NodeId\``
  - 节点注释正文放在对应三级标题下方，允许多行 Markdown
  - AI 读取接口再把节点注释聚合回节点层返回
- 注释映射必须具备宿主节点一致性约束：
  - 删除节点时，关联注释必须同步删除
  - 标准化、导入、AI 改写、批量结构变更后，都必须清理悬挂注释
  - 即使 `AI 锁定注释`，当节点本身被删除时，孤儿注释也必须自动回收，不能残留
- 画布与属性栏必须提供节点注释的可视化编辑入口：
  - 节点属性栏内允许直接编辑当前节点注释
  - 该编辑应同步写回 `## Node Annotations`
- AI 锁定规则也必须覆盖节点注释：
  - 锁定时不允许 AI 主动改写节点注释
  - 但当 AI 删除节点时，其注释必须跟随节点一并删除
- 若希望 AI 更容易理解，可在 AI 语义快照接口中把节点注释合并返回，但不要把它强塞进标准 Mermaid 节点定义。
- 结论：
  - 对标准文件格式：注释应独立存放
  - 对 AI 读取接口：注释可以聚合回节点层返回
  - 对删除与锁定边界：节点是宿主，注释是从属，宿主消失时注释必须跟着消失
  - 这是兼容性、优雅性和维护成本之间最稳的折中

### 12.6 插件接口预留

- 即使 V1 不做插件市场，也要预留插件宿主能力。
- 插件类型建议：
  - `panel`
  - `tool`
  - `transform`
  - `importer`
  - `exporter`

- 插件必须运行在受限上下文中，不直接获取敏感凭据。

## 13. 性能目标

- 单图目标规模：
  - 节点 `<= 3000`
  - 边 `<= 6000`

- 常规编辑目标：
  - 常见团队文档图在主流办公电脑上保持流畅拖拽和缩放
  - 画布操作延迟可接受
  - 大部分操作在本地完成，不依赖服务端即时计算

- 优化方向：
  - 视口裁剪
  - 增量渲染
  - Worker 中执行布局和解析
  - 按需加载源码模式和历史模式
  - 安卓竖屏下优先首屏画布与必要工具，延迟加载次级面板和重型预览
  - 安卓竖屏下应允许弱化动画、阴影和远端 presence 装饰以保持跟手性
  - 低性能设备下自动关闭高成本视觉效果并增加写入节流

- 低性能 ARM 部署目标：
  - 服务端常态下应接近静态站点级资源占用
  - 协同、存储与鉴权优先保证稳定，不承载重计算任务
  - 页面首屏优先加载画布与必要控制，不预先拉起全部重型模块

## 14. 安全与治理

- 企业登录与单点登录预留
- 文档级权限控制
- 操作审计
- 外部工具调用审计
- 插件沙箱
- 文件版本留痕

## 15. 里程碑

- M1
  - flowchart 基础编辑
  - 云端项目与文件打开
  - Markdown / Mermaid 导入导出
  - compat layer 布局恢复
  - 画布模式 / 源码模式切换

- M2
  - subgraph 分组
  - 资源管理器侧栏
  - Project Graph 侧栏
  - 协同编辑
  - 评论与历史版本

- M3
  - Tool API 预留
  - SDK v0.1 文档
  - 布局策略完善
  - 权限与审计

- M4
  - 更多 Mermaid 图类型评估
  - 插件宿主能力开放
  - 企业集成能力完善

## 16. 当前结论

- 这不是一个“把 Mermaid 画图化”的小工具，而是一个“以标准 Mermaid 为内容层、以空间编辑为工作区层、以协同与扩展接口为平台层”的企业内部产品。
- V1 的关键不是功能多，而是先把三件事做稳：
  - 标准 Mermaid 不被污染
  - 画布交互足够快
  - SDK 与扩展接口从第一天就有清晰边界
