# 语法与指令
## 组结构
<!-- 优先建议的写法 -->
@groud:"组1"[@members:("节点A","节点B",@groud:"组2"),@comment:"意思是组1包含了节点A、节点B、组2"]
@groud:"组2"[@members:("节点C","节点D"),@comment:"意思是组1包含了节点A、节点B、组2"]


<!-- 不太好但兼容的写法 -->
@groud:("组1",("节点A","节点B","节点C"))[@comment:"这是一个组叫组1，包含了节点A、节点B、节点C。感觉可视化和嵌套指代不清"]

<!-- 一种更清晰的写法，面向多嵌套 -->
@groud:("组1",(
  @members:("节点A","节点B"),
  @groud:("组2",@members:("节点C","节点D"))
))[@comment:"意思就是组1里面有节点A，节点B,还套了个组2，组2包含了节点C、节点D"]

<!-- 一种简短的多嵌套写法，和上面一样，只是没了@members -->
@groud:("组1",(
  "节点A","节点B",
  @groud:("组2",("节点C","节点D"))
))

## 树结构
<!-- 通用案例 -->
“节点A” -> "节点B" -> |"关系说明"| "节点C" [@comment:"这里表达了一个从节点A到节点B到节点C，并且表达B到C所指的关系"]
    "节点B" -> |"关系说明"| "节点D" [@comment:"这里表达了从节点B指向了节点D，属于一个分支链路，当前附加指令的范围只有从节点B到节点D的这个范围"]
    @subbranch:"节点B" -> |"关系说明"| "节点E" [@comment:"这里表达了从节点A一直到节点E的分支链路,这个范围是会从根源节点直到末端节点"]
    "节点B" -- |"关系说明"| "节点F" [@comment:"这里表示节点B和节点F没有所指关系，只是有联系"]
    "节点B" <- |"关系说明"| "节点G" [@comment:"这是一种节点G指向节点B的写法"]
    "节点G" -> |"关系说明"| "节点B" [@comment:"跟上面一样，只是一种节点G指向节点B的另一种写法，而且也没有加多一条连接线"]
"节点B" -> "节点H" [@comment:"为了规范，不太建议这种无缩进的做法，虽然也支持不会报错，但尽可能不要不注意缩进关系"]
"节点A" -> "节点B" -> "节点D" [@comment:"可以重复描述已有关系，不会新增指向关系，只是附加指令范围不同"]

<!-- 最建议的标准写法案例 -->
"节点A" -> "节点B" -> "节点C" -> "节点I" [@comment:"这里是做xxx的"]
        @subbranch:"节点C" -> "节点H"[@comment:"这里注释和附加指令就是直接从节点A一直到节点H的整个链路了"]
    @subbranch:"节点B" -> "节点D" [@comment:"这里是做xxx的"]
    @subbranch:"节点B" -> "节点E" [@comment:"这里是做xxx的"]
        @subbranch:"节点E" -> "节点F" [@comment:"这里是做xxx的"]
        @subbranch:"节点E" -> "节点G" [@comment:"这里是做xxx的"]

<!-- 也可以，但没那么标准 -->
"节点A" -> "节点B" -> "节点C" -> "节点I" [@comment:"这里是做xxx的",]
        "节点C" -> "节点H"[@comment:"这里注释和附加指令就只是节点C到节点H两个节点组成的链路了，不过子节点连接是一样的，只是附加指令范围不一样"]
    "节点B" -> "节点D" [@comment:"这里是做xxx的"]
    "节点B" -> "节点E" [@comment:"这里是做xxx的"]
        "节点E" -> "节点F" [@comment:"这里是做xxx的"]
        "节点E" -> "节点G" [@comment:"这里是做xxx的"]

<!-- 甚至可以没有缩进，也不建议 -->
"节点A" -> "节点B" -> "节点C" -> "节点I" [@comment:"这里是做xxx的",]
"节点C" -> "节点H"[@comment:"这里是做xxx的"]
"节点B" -> "节点D" [@comment:"这里是做xxx的"]
"节点B" -> "节点E" [@comment:"这里是做xxx的"]
"节点E" -> "节点F" [@comment:"这里是做xxx的"]
"节点E" -> "节点G" [@comment:"这里是做xxx的"]

<!-- 甚至可以这样，但比较憋屈，关系不明显，不建议 -->
"节点A" -> "节点B"
"节点B" -> "节点C"
"节点C" -> "节点I"[@comment:"这样就是注释了只有节点C映射到节点I的超短链路关系了，如果使用别的指令也是只对应这个短链路"]
"节点C" -> "节点H"
"节点B" -> "节点D"
"节点B" -> "节点E"
"节点E" -> "节点F"
"节点E" -> "节点G"

## 时序
@seq:"密码登录"(
  "用户" >> |"POST /login"| "网关"
  "网关" >> |"校验"| "鉴权"
  "鉴权" << |"ok"| "网关"
  "网关" << |"token"| "用户"
  @alt:"失败"(
    "鉴权" << |"401"| "网关"
  )
)

## 思维导图
@mind:"项目路径"(
  "src"["业务代码"]
  "core"["核心引擎"]
    "audio"
    "video"
    "canva2d"["画布"]
  "asset"["这里不要放视频"]
  "其他"
)

## MD
<!-- 可以加入原生MD格式 -->
@md:"文档"(

)

## 基本语法
### 符号
"": 参数，前面什么指令都没的情况下通常指节点@node:""
@: 指令符，后面接指令，例如：@node:"节点A"
(): 里面放数组，或者多参数，例如：("参数a","参数b")
[]: 是前面方指令的附加指令的意思，例如：@node:"节点A"[@todo.prio:"0",@comment:"这个是使用了注释指令注释了节点A，并且前面的指令让这个节点待办优先级为最高"]
->: 节点关系指向，从左往右指，例如："节点A" -> "节点B"
<-: 节点关系指向，从右往左指，例如："节点A" <- "节点B"
--: 节点关系指向符，无指向，但有关系："节点A" -- "节点B"
| |: 关系注释，通常在节点关系指向符后面例如："节点A" -> |"注释"| "节点B" 
,: 在括号内是分隔开单位或指令，在双引号内是普通字符
\n: 换行符
\\: 转义符，例如：\\n

### 指令语法
<!-- @后面使用的是指令或数据类型，完整格式案例@node:"xxx"[]，如果[]里面没有都没有，可以不加 -->
<!-- 下面为一些案例 -->
<!-- 无附加指令，单参数 -->
@指令:"参数"
<!-- 无附加指令，双参数 -->
@指令:("参数1","参数2")
<!-- 无附加指令，三参数 -->
@指令:("参数1","参数2","参数3")
<!-- 无附加指令，双参数带数组 -->
@指令:("参数1",("参数2","参数3"))
<!-- 单参数，有一个附属指令 -->
@指令:"参数"[@指令:"内容"]
<!-- 单参数，有两个附属指令 -->
@指令:"参数"[@指令:"内容",@指令:"内容"]
<!-- 可以使用换行符 -->
@指令:"内容\n后续"
<!-- 数组或者多参数指令的参数放在()内使用，无指令需用双引号 -->
@指令:("参数1","参数2")
@指令:("参数1",("参数2"))[@comment:"多个括号里面单个和上面一样意思"]


## 指令
### 节点类
<!-- 使用@node:""定义节点 -->
@node:"一个新节点"[@comment:"你创建了一个叫[一个新节点]的节点"]
"一个新节点"[@comment:"另一种写法，不需要写，和上面的一样，就是@node的简写"]
@node:"!@#$%^&*()_+"[@comment:"节点名字可以是任何符号，除了<\">这个符号，前面要加\符号去转义"]
"新节点"[@comment:"这个是直接省去@node的简写，作用一样"]

<!-- 后面可以加上[]，里面可以加上关键字，也可以不加，不加就相当于没有 -->
@node:"自定义节点"[@comment:"这个是自定义节点"]
"自定义节点"[@comment:"这个是节点的简写"]
"自定义节点"["注释也可以简写，但通常不建议"]

<!-- 相关案例解释 -->
<!-- 案例一，如果重复定义节点，其实相当于只是定义了一个节点 -->
@node:"节点"
@node:"节点"[@comment:"这里相当于又定义了一次，但是无所谓，实际上还是同一个"]

<!-- 案例二，如果需要同名节点要怎么做？ -->
@node:"节点"
@node:"节点"[@comment:"这样是非法操作，本质上还是之前那一个，可以使用下面解法"]
@node:"节点#2"[@comment:"可以加上编号，自己定义的编号，不会和之前的节点冲突"]
@node:"节点组.节点"[@comment:"表示组结构，这个最建议"]

### 组类
@groud:("组1",("节点A","节点B","节点C"))[@comment:"命名了一个叫组1的组，里面包含3个分别叫(节点A，节点B，节点C)的节点,阅读性较差，不建议使用"]
@groud:"组1"[@comment:"这里有一个组叫组1，但是没有说明包含节点"]
@groud:("组2")[@comment:"这里代表"]
@groud:"组1"[@members:("节点A","节点B")[@comment:"作为附加节点使用，通常配合groud使用，也可以配合node使用，比较建议的语法"]]

### 其他类
@url:"www.baidu.com"[@comment:"这个可以放超链接可以跳转哦"]

### 注释类
@comment:"这个放归属于行内内容的注释，通常不会像这样用在节点内"
["这样直接一个中括号加双引号是@comment的简写，实际上和上面的@comment是一样的，但是上面的更规范，建议使用上面的@comment"]

### todo类
<!-- 类的子函数后面用"." -->
@todo.message:"待办信息注释"[@comment:"这里是待办的注释，包含返回接口"]
@todo.prio:"0" [@comment:"待办层级，内部直接用数字，0为最高优先级，数字越大优先级越低"]
@todo.status:"active"[@comment:"active表示正在做"]

### ai类
<!-- 第一刀：解析保留原文，不执行 -->
<!-- 这里要重构，想想如何实现持久性任务交互与复用 -->
@ai.running:"产品经理agent"[@comment:"意思是当前一个叫ai的名字叫产品经理agent正在运行到这里"]
@ai.agent.crea:"营销大师"[@comment:"意思是在这里创建一位叫营销大师的agent"]
@ai.subagent:("数据分析小助手","你会去参考数据写一份分析报告")[@comment:"意思是这个组/节点/链路专门创建一个叫数据分析小助手的agent，prompt为\"你会去参考数据写一份分析报告\"，并且这个agent可以在当下执行"]
@ai.sentry:("更新agent","更新")[@comment:"一个专门维护该节点的ai"]


### cli执行类
<!-- 第一刀：解析保留原文，打开文件时不执行 -->
@cli.new:"终端线程名字"[@comment:"创建了一个名为new的终端任务"]
@cli.command:("pwd","new")[@comment:"该节点执行终端指令，当前意思是在名为new的终端任务中执行pwd命令，并且返回数据到接口，如果没有第二个参数，就是临时的新线程执行一次返回数据就销毁"]
@cli.ai:"你好"[@comment:"该节点在cli执行"]


### 节点图语法案例：
<!-- 使用"->"表达关系 -->
"节点A" -> "节点B"[@comment:"这个注释是在一条整条关系链路，意思是节点A指向B"]
"节点A" -> "节点B" -> "节点C"[@comment:"一条长链，这个注释是针对一整条长链的"]

<!-- 使用-> |""|表达关系说明 -->
"节点A" -> |"衍生出"| "节点B" [@comment:"这个注释是在一条整条关系链路，意思是"]

<!-- 使用@subbranch:""表达分支关系 -->
"节点A" -> "节点B" -> "节点C"  [@comment:"链路a注释"]
  @subbranch:"节点B" -> "节点D" -> "节点E" -> "节点F"  [@comment:"从B点出来的分支"]
    @subbranch:"节点D" -> "节点G"  [@comment:"从D点出来的分支"]

<!-- 下面的语法也可以，但不规范，不建议 -->
"节点A" -> "节点B" -> "节点C"
"节点B" -> "节点D" -> "节点E"  [@comment:"相当于从B点出来的分支"]

<!-- 表示一个回环的节点 -->
"节点A" -> "节点B" -> "节点C" -> "节点A" [@comment:"最后回到节点A的循环链路"]
"节点A" -> "节点B" -> "节点C" -> @backto:"节点A" [@comment:"最规范的语法"]


# 插件依赖层
<!-- 第一刀：解析保留 / 不安装、不执行 -->
<!-- 用户对当前lmd的第三方插件依赖都在这里，会被lmd编辑器检测安装 -->
@plugin:"插件名字"[@comment:"插件作用说明",@type:"插件类型",@url:"插件仓库地址",@version:"插件版本",@function:("插件指令1","插件指令2")]

# 插件附加层
<!-- 用户对当前lmd的插件指令都在这里，会被lmd编辑器检测安装 -->

# 附加插件层
<!-- 插件的附加功能类都在这里 -->


# agent行为分区
<!-- 第一刀：不实现 -->
<!-- 当前的节点下AI的状态 -->
nownode:"节点B"
任务完成情况:""[@comment:"当前任务的完成百分比"]
[@comment:"当前节点的子计划与完成情况"]


<!-- 最临近路线图 -->
@lastnode:"节点A" -> @nownode:"节点B" -> @lastnode:"节点C"
        @nownode:"节点B" -> @lastnode:"分支节点D"

<!-- 下一个分支预览 -->
@lasenode:(
    "节点C"[@if:"成功率小于5/10"[@comment:"去这个节点的理由，也就是关系指向|\"\"|里面的内容"],@comment:"节点C的注释"],
    "节点D"[@if:"成功率大于5/10"@comment:"节点C的注释"],
)

<!-- 当前完整路线 -->


# cli指令
<!-- 第一刀：不实现 lmd-cli 地图交互 -->
<!-- 终端使用命令行lmd-cli可以进入cli模式 -->
## 地图交互类
获取周边地图与当前路径节点
获取当前节点的提示词和操作
返回全部节点的关系图
返回所有嵌套关系
前进选择
重试

## 节点操作类
把多个节点成组
新建节点
新建节点并指向节点B
新建节点并让节点B指向刚刚到节点
解散组




# 插件系统
<!-- 第一刀：不实现运行时 -->
## lmd文件拓展包


允许用户可以在lmd文件中使用自定义的插件，插件的语法为@插件名:("参数1","参数2")


## UI显示
允许用户可以在节点的下拉展开显示自定义UI显示
可以允许xx通用网页显示协议做插件

## 执行类
cli执行类允许
python插件执行
Node.js插件执行
ai插件执行

