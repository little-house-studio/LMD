/** Default display skin. Semantic content must not depend on these values. */

export const DEFAULT_NODE_THEME = {
  fill: '#121214',
  stroke: '#d6ff3a',
  textColor: '#f4f4f5',
} as const;

export const DEFAULT_GROUP_THEME = {
  fill: '#141418',
  stroke: '#00f0ff',
  textColor: '#f4f4f5',
} as const;

export const DEFAULT_EDGE_THEME = {
  strokeColor: '#8a8a94',
  strokeWidth: 1.5,
} as const;
