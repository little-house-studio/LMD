/**
 * @lths/lmd — composition root.
 * Bounded contexts live in `shared-kernel`, `document`, `display`, `diagnostics`,
 * `editing`, `layout`, `runtime`, `plugin`.
 */
export * from './composition';
